# shiftcrew-owner — Owner App

> ר' גם `/Users/homestation/Desktop/CLAUDE.md` להקשר הכללי (Supabase, deploy, סטטוס,
> ובמיוחד "עבודה מקבילה על אותם ריפואים" — **קרא את זה קודם**).

## ⚠️ מה זו התיקייה הזו, בדיוק (עודכן 2026-08-11)

זו האפליקציה החיה, מסונכרנת עם `origin/main` (`github.com/ezergalit/shiftcrew-owner.git`).
יש גם `shiftcrew-owner-rebuild/` — **אותו repo בדיוק**, clone נוסף, נכון לעכשיו גם הוא
מסונכרן לאותו commit. אין ביניהם הבדל תוכן כרגע; אל תניחו שאחד "יותר מעודכן" מהשני בלי
לבדוק `git log` בפועל — ר' `/Users/homestation/Desktop/CLAUDE.md` ("סטטוס 2026-08-11")
לפרטים מלאים על commits בשם `Eytan Shleizer` שהופיעו ב-`origin/main` תוך כדי עבודה על
אותו קובץ (כולל דיון פתוח אם זו זהות git מקומית או session מקביל אמיתי).

**כדי לדפלוי**: `git push` מהתיקייה הזו. **אל תעשה commit/push בלי אישור מפורש מהמשתמש**
(כלל כללי). **תמיד `git fetch` ובדוק `git log HEAD..origin/main` לפני שמתחילים לערוך** —
יש עוד מישהו שעובד על הריפו הזה במקביל.

## 🖥️ «אורורה» — עור אפליקציית הניהול (2026-08-28)

העיצוב שאושר בצילומי המסך של יותם, מולבש על צד המנהל **פר-מסעדה** מאחורי אותו דגל
שהמלצר כבר קורא — `restaurants.features`:

| דגל | מה הוא עושה בצד המנהל |
|---|---|
| `design:"aurora"` | רקע אורורה + זכוכית + סרגל אימוג'י (🏠 📖 ⚙️) עם קו אמרלד זוהר. **הסרגל העליון נעלם לגמרי** |
| `tasks:false` | אין משימות, אין בריף, אין צ׳קליסטים. הבית = מצב התפריט + מי לומד |

חי ב-`SALON26` וב-`STUDIO26`. כל שאר המסעדות — **כולל `CREWDEMO`, חשבון הבודקים של
אפל ושל גוגל** — ממשיכות לראות בדיוק את האפליקציה הישנה.

### שלושת המסכים

| טאב | קובץ | מה יש בו |
|---|---|---|
| בית | `components/aurora/OwnerHome.jsx` | ברכה + «תצוגת מלצר» · 3 אריחים (שליטה · מנות · למדו היום) · **בריאות התפריט** · **מה הצוות מתקשה בו** · «מי למד היום» + הודעה לכל הצוות |
| תפריט | `components/aurora/OwnerMenu.jsx` | כותרת + «מנה חדשה» · חיפוש · צ׳יפי תפריט וקטגוריה · כרטיסי מנה **עם תצלום** |
| הגדרות | `components/aurora/OwnerSettings.jsx` | קוד הצטרפות ענק + שיתוף בוואטסאפ · הצוות · מסלול הלמידה · המסעדה · שש הגדרות מתקפלות · יציאה |

`components/aurora/bits.jsx` מחזיק את האטומים המשותפים (`Tile`, `MemberLine`,
`Section`, `Choice`, `membersLabel`, `initials`, `pctColor`).

### שני המסכים הנגזרים בבית — הם כל הרעיון

עיקרון «אוטומציה לפני עבודה» (פנקס השותף #8): המנהל לא ממלא כלום, הוא **מאשר ומתקן**.
- **בריאות התפריט** — מנות בלי תיאור, מנות בלי אלרגיות, וכמה מוכנות ללימוד. מנה אחת
  ⇒ נפתחת המנה; כמה ⇒ נפתח מסך התיקון הקבוצתי, כי לחיצה על עשרים מנות אחת-אחת היא
  בדיוק העבודה שאנחנו אמורים להוריד.
- **מה הצוות מתקשה בו** — נגזר מ-`menu_progress`. **הקשה מדגישה ⭐ את המנה מיד**,
  בלי לפתוח טופס.

⚠️ **שני כללי גזירה שנלמדו בפועל:**
1. **כרטיסי ידע אינם מנות.** קטגוריה שמתחילה ב-`הדרכת` או שם שמתחיל ב-`מה חשוב לדעת`
   (אותו כלל כמו `pubToCard` אצל המלצר). ל-SALON26 יש 10 כאלה ול-STUDIO26 17, ובלי
   ההחרגה הם נספרו כ"מנות בלי אלרגיות" — 21 שורות אדומות מבהילות, אף אחת מהן אמיתית.
   הם גם מוחרגים מספירת «מנות בתפריט» (72⇒62, 152⇒135).
2. **«מתקשה» הוא mastery ≤ 2, לא «כל מה שלא נשלט».** דירוג 3 הוא מלצר שבאמצע הלמידה;
   לספור אותו כטעות ממלא את הכרטיס בכל מה שהצוות לומד כרגע.
   ובלי **שני** אנשים אין שבר: «1 מתוך 1 טועים» הוא אדם אחד שעוד לא למד. מתחת ל-2
   מנסים המילים «עדיין לא יודעים».

### 🔴 הבאגים שנתפסו בסבב הזה

1. **סרגל הניווט עיצב כל כפתור באפליקציה.** ה-div של הסרגל התחתון **עוטף גם את כל
   המודאלים** (גיליון העובד, מסך הצוות, הסיור המודרך) — מבנה קיים שלא הפריע כשהוא היה
   רקע אפור. ברגע שהוא קיבל `.au-nav` עם `flex-direction:column` על כל כפתור צאצא,
   כל כפתור בכל דיאלוג נשבר לשתי שורות («הבא» מעל החץ). תוקן בהצמדת הכללים ל-
   **`.au-tabs > button`** (מחלקה על שורת הטאבים עצמה). ⚠️ **ה-build היה ירוק.**
2. **הסיור המודרך תיאר אפליקציה שלא קיימת.** `buildSteps` דיבר על משימות היום,
   העדכון היומי, הצ׳קליסטים וכפתור ה-📊 — כולם הוסרו ע"י `tasks:false`. נוסף
   `buildAuroraSteps` בן 5 תחנות שמתאר את המסכים האמיתיים, והצבע עבר מתכלת לאמרלד.
   זו **הפעם השלישית** שהסיור מתיישן אחרי שינוי ניווט — ר' האזהרה שכבר כתובה כאן.
3. **גלילה לא התאפסה במעבר טאב.** שלושת הטאבים חולקים scroller אחד, אז «הגדרות»
   נפתח באמצע העמוד. `scrollRef` + `useEffect` על `tab`. ⚠️ **מוגבל ל-aurora בכוונה** —
   בזמן סבב בודקים גם שיפור הוא שינוי.

4. 🔴 **הבאג הכי חמור, ולא הייתי מוצא אותו בעין: `backdrop-filter` כלא את כל המודאלים.**
   ה-div של הסרגל התחתון עוטף גם את `MemberSheet`, `TeamMessageDialog`, `ProfileGate`,
   `TeamScreen` והסיור — כולם `fixed inset-0`. **כל ערך של `backdrop-filter` שאינו `none`
   הופך אלמנט ל-containing block עבור צאצאי `position:fixed`**, ולכן כולם נמדדו מול
   סרגל בגובה 108px במקום מול המסך. נמדד: הגיליון חזר **375×108** במקום 375×812.
   תוקן בהוצאת המודאלים החוצה — הם **אחים** של הסרגל, לא ילדים שלו.
   ⚠️ שימו לב שזה **אותו קינון** שגרם לבאג #1; הפעם הראשונה תוקנה בהצמדת סלקטור,
   וזה הסתיר את הבעיה השנייה. **כשמגלים קינון מפתיע — לבדוק את כל ההשלכות שלו, לא רק
   את התסמין שראיתם.**

5. **`exam_config` נדרס מזיכרון ישן.** כרטיס מסלול הלמידה טען את השורה **פעם אחת**
   ומיזג אותה לכל שמירה — בזמן ש-`LearningPathSettings`, שכותב `facets` ו-
   `category_order`, יושב במקטע מתקפל **באותו מסך**. עריכה שם ואז הקשה על צ׳יפ למעלה
   הייתה מחזירה את המערכים הישנים. עכשיו קוראים את השורה מחדש בתוך כל שמירה.

6. **שני בוררים לאותה עמודה הציעו ערכים שונים** — `pass_threshold` היה `[40,50,70]`
   בכרטיס ו-`[30,50,70]` במסך המתקדם, ושניהם נגישים מאותו טאב. יושרו.

7. **שמירה שנכשלה השאירה את הערך החדש על המסך** ⇒ המנהל היה יוצא בטוח שהשינוי נשמר.
   עכשיו מתגלגל אחורה.

8. **`Dish` הוגדר בתוך הרנדר** של `OwnerMenu` — טיפוס קומפוננטה חדש בכל הקשה, כלומר
   React פירק ובנה מחדש את כל 152 הכרטיסים (כולל התמונות) בכל תו בחיפוש. הוצא החוצה.

### 🔴 «הכלא של backdrop-filter» — הבאג שחזר ארבע פעמים (29.8)

**כל ערך של `backdrop-filter` שאינו `none` הופך אלמנט ל-containing block עבור צאצאי
`position:fixed`.** העור בנוי על משטחי זכוכית, ולכן כל שכבה `fixed inset-0` שנוצרת
בתוך כרטיס נמדדת מול הכרטיס במקום מול המסך. ארבע פעמים, כולן עם **build ירוק**:

| # | מה נכלא | נמדד | התיקון |
|---|---|---|---|
| 1 | כל המודאלים של הניהול (גיליון עובד, דיאלוג הודעה, שער פרופיל, סיור) — היו ילדים של `.au-nav` | 375×**108** | הוצאו החוצה כאחים של הסרגל |
| 2 | הגדלת תצלום המנה בטופס העריכה | 333×**1322** | `createPortal` ל-`body` |
| 3 | דיאלוג ההתנתקות של **המלצר** — ילד של ההדר, שנושא `backdropFilter` | 375×**56** | `createPortal` ל-`body` |
| 4 | פס «יש שינויים שלא נשמרו» של מסלול הלמידה | `top:3712` — שלושה מסכים מתחת לקפל | `createPortal` + `bottomOffset` |

⚠️ **#4 היה הגרוע ביותר**: המנהל שינה הגדרה מתקדמת וכפתור השמירה היה מחוץ למסך,
כלומר **השינוי פשוט לא נשמר** ולא הייתה שום אינדיקציה. ⚠️ **#3 פוגע במלצרים
אמיתיים** ב-SALON26 וב-STUDIO26.

**הכלל**: לפני שמוסיפים `fixed inset-0` — לבדוק אם יש אב עם `backdrop-filter`/`filter`/
`transform`. הבדיקה בדפדפן:
```js
const p=document.createElement('div'); p.style.cssText='position:fixed;inset:0';
ancestor.appendChild(p); p.getBoundingClientRect(); // ≠ המסך ⇒ נכלא
```

### 🔧 סבב הערות יותם (29.8)

| הערה | מה נעשה |
|---|---|
| «חסרות אלרגיות · 24 מנות» לא שלח לשום מקום | השורות בכרטיס הבריאות פותחות עכשיו את **התפריט מסונן בדיוק לאותן מנות**, עם באנר ו«כל התפריט» ליציאה. מסך התיקון הקבוצתי נשאר בהגדרות |
| אי אפשר להגדיל תמונת מנה בעריכה | תמונה 64px לחיצה ⇒ מסך מלא (portal) |
| אין הסבר צבעים למנהל | מקרא בן שלושה צ׳יפים בראש טאב התפריט: 🔴 אלרגיות · 🟣 🤰 רגישות · 🟡 מוקשים |
| ההודעות לצוות מזכירות עדכון יומי | `SUGGESTIONS_MENU_ONLY` — שישה נוסחים בלי בריף ובלי משמרות, נבחר לפי `tasksOff` |
| יציאה מתצוגת המלצר עם טיימר 5 שניות | בתצוגה הכפתור יוצא **מיד**; המלצר שולח `postMessage` והניהול סוגר את החלון |
| אין אפשרות להוסיף תמונה למנה חדשה | Edge Function `dish-photo` — ר' למטה |
| אין כפתור יציאה למעלה בטופס מנה | כותרת «מנה חדשה»/«עריכת מנה» עם ✕, והעריכה הפכה **למסך מלא** במקום טופס מעל הרשימה |

### 📷 העלאת תצלום מנה — Edge Function `dish-photo`

האפליקציות מדברות עם Postgres כ-`anon` ומוכיחות זהות בכותרת `x-app-session`.
**Storage הוא שירות אחר ולא רואה את הכותרת**, ולכן ה-policies שלו מתירים רק
`authenticated` — תפקיד שהמוצר לא משתמש בו. פתיחת Storage ל-`anon` הייתה נותנת לכל
מי שמחזיק את ה-anon key (הארוז ב-bundle) לכתוב לדלי.

הפונקציה מאמתת את הסשן בצד השרת ומעלה עם ה-service key.
⚠️ **הנתיב נגזר מהסשן ולא מגוף הבקשה** — מנהל יכול לכתוב רק תחת התיקייה של המסעדה שלו.
- `menu_app.session_owner_for_token(text)` — SECURITY DEFINER, `grant execute` ל-**service_role בלבד**. הטבלה `app_sessions` נשארת בלי שום grant.
- ⚠️ נדרש גם `grant usage on schema menu_app to service_role` — בלעדיו: `permission denied for schema menu_app`.
- ⚠️ **`x-app-session` חייב להיות ב-`Access-Control-Allow-Headers`**. הלקוח מזריק את הכותרת לכל בקשה, ולכן הדפדפן עושה preflight — **curl לא עושה preflight, אז זה עבר כל בדיקה בשורת הפקודה ונכשל באפליקציה.**
- הכתיבה ל-`menu_items.image_url` נעשית **בלקוח**, בנתיב הרגיל, כדי ש-service_role לא יקבל גישת כתיבה לתפריט.
- ⚠️ תמונה שהועלתה ואז בוטלה נשארת ב-Storage. אין ניקוי.

### 🔴 מנה חדשה נעלמה מהצוות (29.8) — באג שקט

`handleSaveDish` **מעולם לא כתב `menu_group`**. במסעדה עם יותר מתפריט אחד — סטודיו,
חמישה — הערך היה `NULL`, והמלצר מסנן `inMenu: c.menuGroup === menu`, כלומר **המנה
לא הופיעה לאף אחד בצוות** בזמן שאצל המנהל היא נראתה מצוין. נגזר עכשיו מהקטגוריה
(`menuGroupForCategory`; אומת ש-קטגוריה⇄תפריט הוא 1:1 בשתי המסעדות), והטופס מציג
«יופיע ב…». בנוסף: מנה חדשה יורשת את הקטגוריה המסוננת, ואם אין — **הראשונה שאינה
כרטיס ידע** (קודם היא נחתה ב«הדרכת שירות»).

### 🔢 שני מסכים שסתרו זה את זה

«בריאות התפריט» בבית ו«בדיקת בריאות התפריט» בהגדרות ספרו אחרת את אותו דבר —
11 מול 21 בסלון, 24 מול 75 בסטודיו. `MenuHealthReview` קיבל את אותם קלטים
(בלי כרטיסי ידע, ועם `needsAllergens` שמחריג משקאות). גם `MemberSheet` אמר
«0/152 מנות» ליד אריח שאומר «135 מנות» — המכנה נכון (הוא סופר כל פריט נלמד),
**המילה** לא: תחת `tasksOff` היא «פריטים».

### 🔎 עיון במנה לפני עריכה (29.8)

הקשה על מנה פתחה את טופס העריכה המלא — כל השדות בבת אחת, כשכל מה שהמנהל רצה הוא
לבדוק מה יש בה (יותם: *"it opens everything instead of a summary"*). קריאה ועריכה הן
שתי מלאכות: `DishPreview` הוא הקריאה — תצלום, תיאור, מרכיבים, ואזהרות בשלושת הצבעים —
ובתחתיתו כפתור אחד לעריכה.
⚠️ **מיקום הגלילה נשמר** ביציאה מהמנה. בלעדיו כל הצצה במנה מחזירה את המנהל לראש
רשימה של 152 מנות, וזה בדיוק מה שהפך "לעבור על התפריט" לבלתי אפשרי.

### ⚙️ אורך בוחן ההיכרות (29.8)

`baseline_minutes` נוסף לכרטיס מסלול הלמידה (3/5/7/10/15 דק׳), ומוצג **רק כשהבוחן
דלוק** — אורך למבחן שאיש לא ניגש אליו הוא עוד בורר שלא עושה כלום.
המלצר קורא אותו ב-`BaselineIntake.jsx:77`.

⚠️ **הדיווח "אי אפשר לבטל את בוחן ההיכרות" לא היה באג בשמירה** — נבדק גם בשרת (upsert
עובר) וגם בלקוח (הטוגל נשמר ומציג "נשמר ✓"). זה היה **פס השמירה של המסך המתקדם**,
שנמדד `top:3712` ולכן היה מחוץ למסך. ר' «הכלא של backdrop-filter».

### 🔗 כל הגדרה של המנהל נקראת אצל המלצר — נבדק

| הגדרה | מי קורא אותה |
|---|---|
| `pass_threshold` | `lib/learningPath.js` · `MainApp` |
| `general_exam_questions` | `MainApp` |
| `baseline_enabled` | `App.jsx` |
| `baseline_minutes` | `BaselineIntake.jsx` |
| `gate_games` | `lib/learningPath.js` |
| `facets` | `BaselineIntake` · `MainApp` · `QuizExam` |
| `category_order` | `MainApp` · `learningPath` · `serviceScenarios` · `MenuExam` |

### איך מוכיחים שלא נגענו באף מסעדה אחרת

`Desktop/aurora-owner-qa/` — `shot.html` (דרייבר same-origin ש**חייב לשבת ב-`public/`**;
`vite build` מוחק את `dist` ואיתו כל מה שהעתקתם לשם), `shoot.sh` (Chrome headless,
חלון 460×2600 כדי שה-`max-w-md` של האפליקציה הלא-מעוצבת ייכנס במלואו), `diff.py`.
משווים **build של `origin/main` מול הענף**, שניהם מקומיים, על אותם נתונים חיים.
⚠️ **תמיד למדוד רצפת רעש** (אותו build מול עצמו). עם המתנה ל-DOM יציב הרצפה היא **0
פיקסלים**, ולכן כל הפרש הוא אמיתי — עם `--virtual-time-budget` היא הייתה אלפי פיקסלים
של פסי התקדמות באמצע אנימציה.
⚠️ **מלכודת חצות**: הרצה שחוצה את חצות מייצרת דיף ענק (88K פיקסלים) שאינו רגרסיה —
התאריך, «משימות היום» וסימוני האתמול משתנים בין שני הצילומים. לצלם שוב, לא לתקן.

**התוצאה בפועל** (28-29.8): `CREWDEMO` ו-`87EE49` — **0 פיקסלים** בכל שלושת הטאבים,
מול רצפת רעש של 0.

⚠️ **שני דברים שהמעבר הזה תלוי בהם — נתונים, לא דגל**: `dishFromDb` מעביר עכשיו
`image_url`, מה שמפעיל תמונונת 36px שכבר הייתה כתובה בכרטיס הלא-מעוצב; ואף מסעדה
לא-מעוצבת לא מחזיקה תצלומים, ולכן זה רדום. אם אי פעם תיווסף — היא תראה תמונה. זה
תיקון, לא רגרסיה, אבל הוא מותנה בנתונים ולא בדגל. (התצלום בטופס העריכה **כן** מאחורי
הדגל.)

## ארכיטקטורה נוכחית (2026-08-11)

```
main.jsx → App.jsx
              │
              ├─ VITE_DEV_BYPASS_AUTH=true (.env.local, gitignored) → דמו מקומי, מדלג login
              ├─ session ב-localStorage ("menu-app-owner-session") + restaurant מלא
              │   מ-DB (RESTAURANT_COLUMNS מיוצא מ-OwnerDashboard.jsx — לא כולל
              │   owner_password/owner_password_hash, חסומים ב-DB ברמת עמודה)
              │   ├─ אין/לא תקף → OwnerLogin
              │   └─ תקף → OwnerDashboard
              │
OwnerLogin.jsx:
  הרשמה פתוחה לגמרי (שם+סיסמה, ללא שער admin) + כניסה (קוד+סיסמה), שתיהן דרך
  RPC ב-DB (`supabase.schema("menu_app").rpc("create_restaurant_account"/"verify_owner_login")`).
  ר' CLAUDE.md הראשי, סעיף "רב-דיירות", לפרטי ה-RPCs עצמם.

OwnerDashboard.jsx (~1150 שורות אחרי הגרסה של Eytan מ-2026-08-11):
  - אונבורדינג בכניסה ראשונה (מוגדר לפי `!restaurant.onboarding_completed`, **לא** לפי
    `!description` יותר):
    1. פרטי מסעדה מצומצמים (שם + CuisineSelector + תיאור — הוסרו כתובת ודירוג-חשיבות
       אלרגנים)
    2. מדריך הזנת תפריט: הדבקת תפריט שלם כטקסט חופשי → parser היוריסטי מפרק לקטגוריות
       (כולל קטגוריות מפוצלות כמו "ראשונות קרות/חמות") ומנות, הבעלים עובר/עורך במסך אחד,
       שומר בבת אחת (מתפרסם אוטומטית ל-waiter דרך ה-trigger הקיים)
  - קטגוריות הן טקסט חופשי per-restaurant, לא רשימה קבועה של 4 ערכים יותר
  - Bottom nav (5 טאבים): בית | תפריט | פרטים | צוות | יציאה
  - בית: עורך Daily Brief (חסרים/חדש/בתנור/הערות — נקרא חי ב-waiter app)
  - תפריט: CRUD מלא למנות (`menu_items`)
  - פרטים: תצוגה+עריכה של כל שדות המסעדה
  - צוות: קוד הצוות לשיתוף + כרטיס לכל חבר צוות עם **אחוז ידע אמיתי** (לא ספירה),
    סרגל התקדמות צבעוני, **שמות המנות שהוא טועה בהן**, כמה מנות עוד לא נגע בהן,
    והאם השלים אתגר יומי / קרא עדכון יומי.
    📊 **תוצאות מבחנים** — צ'יפים ירוקים/אדומים ("קינוחים 100% ✓" / "ראשונות 34% ✗")
    מטבלת `menu_app.exam_results` (נוצרה 2026-08-12; שורה אחת לכל מבחן שהושלם, נכתבת
    ע"י ה-waiter). מוצג **המבחן האחרון בכל קטגוריה** — ההיסטוריה המלאה נשארת בטבלה.
    `CAT_LABELS` ממפה את מפתחות הקטגוריות הישנים (`starters`→ראשונות); מסעדות שבנו תפריט
    דרך ייבוא/AI משתמשות בשמות עברית חופשיים שעוברים as-is.
    ⚠️ **למה אחוז ולא ספירה (תוקן 2026-08-12)**: הטאב הציג `leaderboard.mastered_count` —
    ספירת מנות שעברו 4/5 — וזה **הפך את הדירוג**: עובד עם 9 מנות על 4 בדיוק (56%) נראה
    טוב יותר מעובד עם 8 מנות על 5 (75%). זה בדיוק המדד שהוחלף באפליקציית המלצרים.
    הפתרון: הטאב שולף `menu_progress` ישירות (`.in("team_member_id", memberIds)`) ומחשב
    `sum(mastery) / (מנות × 5)` — אותה נוסחה בדיוק כמו `scorePct` ב-waiter, כדי ששני
    הצדדים לא יסתרו. `leaderboard` נשאר לנקודות/אתגר-יומי בלבד; **הוא לא מכיל סכום, רק
    ספירה**, ולכן אי אפשר לחשב ממנו אחוז — אל תנסו.
    ℹ️ אין realtime בטאב הזה — טעינה חד-פעמית ב-mount; רענון ידני מספיק.
  - "אנשי צוות ניהוליים" (owner_users) — כמה משתמשי-בעלים בשם, מוצג בטאב פרטים/הגדרות
  - **אין יותר "פרסום" ידני** — trigger ב-DB (`trg_sync_published_menu`) משקף כל שינוי
    ב-`menu_items` ל-`published_menu` אוטומטית ומיידית

CuisineSelector.jsx: קומפוננטה עצמאית לשימוש חוזר (אונבורדינג + פרטים), חיפוש+autocomplete+
  תגיות ניתנות להסרה + "הוסף כסוג מותאם אישית" אם לא נמצא match.
```

## מסלול הלמידה — מה הבעלים שולט בו (2026-08-12)

```
src/lib/examFacets.js                  — מטא-דאטה של מה אפשר לבחון + ההמלצה שלנו
src/components/LearningPathSettings.jsx — טאב "הגדרות": דירוג + סדר + ספים
src/components/MenuHealthReview.jsx     — טאב "הגדרות": תיקון תפריט בכמות
src/components/ProgressChart.jsx        — טאב "צוות": גרף שיפור פר-מלצר
```

- **`examFacets.js` הוא שיקוף של `FACETS` ב-`shiftcrew-waiter/src/lib/questionEngine.js`.**
  שני ריפואים נפרדים = אין package משותף. **שינוי facet חייב לקרות בשני הקבצים**, אחרת
  הבעלים ידרג משהו שאפליקציית המלצרים לא יודעת לשאול. המפתחות הם החוזה — הם מה שנשמר
  ב-`exam_config.facets`.
- **מציעים רק מה שהתפריט תומך בו** (`facetsForMenu`), וכל אופציה חסומה מסבירה מה חסר.
  הכל מאותחל להמלצה שלנו ומסומן ככזו.
- **`MenuHealthReview` משתמש באותם predicates** של `examFacets` — כך שמה שהמסך מדווח
  כחסר הוא בדיוק מה שחוסם שאלות טובות יותר. פעולות בכמות: סימון/הסרת אלרגן לקבוצה,
  העברת קטגוריה. ⚠️ אחרי פעולה שמכווצת קבוצה — הבחירה נגזמת (אחרת "נבחרו 3 מתוך 1").
- **`dishFromDb` חייב לכלול `ingredients`** — הוא לא כלל אותם עד 2026-08-12, ולכן
  הבעלים לא יכול היה לראות/לערוך מרכיבים ומסך ההגדרות חשב שאין בתפריט מרכיבים בכלל.

## ⏱️ יעד לימוד יומי + שמה האמיתי של הפתיחה המדורגת (2026-08-14)

`LearningPathSettings.jsx` — שני שינויים:

- **"יעד לימוד יומי"** (5/10/15/20, ★ על 10) ⇒ `exam_config.daily_goal_minutes`.
  המלצר רואה אותו כטבעת בבית ("0/15 דקות מהיעד היומי"). נבדק מקצה לקצה.
  ⚠️ `daily_goal_minutes` נוסף ל-`DEFAULT_PATH` ב-`examFacets.js` **וגם** ל-effect
  הטעינה — ה-`save` עושה `...path`, אבל הטעינה קוראת שדה-שדה, ובלי השורה הזו
  כל שמירה הייתה דורסת את הבחירה חזרה ל-10.
  ℹ️ ברירת המחדל משוכפלת בשלושה מקומות: `DEFAULT_PATH`, ה-`DEFAULT` ב-DB,
  ו-`DEFAULT_DAILY_MINUTES` באפליקציית המלצרים. שינוי ⇒ בשלושתם.

- ⚠️ **"פתיחה מדורגת" שונה ל-"תרגול לפי מה שנלמד"** — הכיתוב הבטיח נעילה שכבר לא
  קיימת ("לומד חלק, נבחן, ורק אז נפתח הבא"). מאז שכתוב `learningPath.js` הדגל
  `gate_games` קובע **היקף** ולא **גישה**: אם התרגול נבנה רק מקטגוריות שהמלצר עבר
  בהן מבחן, או מכל התפריט. הכיתוב עכשיו מתאר את זה. **הדגל עצמו לא שונה** — רק
  התיאור, שהיה שקר.
  בעקבות זה גם הוצא סף המבחן מתוך `{path.gate_games && ...}` — הוא חל בשני המצבים.

## 🔤 לקסיקון האלרגיות (2026-08-16, החלטת משתמש) — v24 פרוס

"דגים" הוסר מערכי האלרגיה (דג נא ⇒ pregnancy בלבד) ו"חלב" ⇒ "לקטוז". החלטת מוצר
של המשתמש. שונה ב: `OwnerDashboard.jsx`, `MenuHealthReview.jsx`, `lib/dishFlags.js`,
ובפרומפטים + `FLAG_VALUES` של `menu-ai-parse` (**v24 פרוס דרך MCP, אומת חי**:
פסטה אלפרדו ⇒ `[לקטוז, גלוטן]`, סשימי ⇒ `allergens:[] pregnancy:[דג נא]`).
מיגרציית נתונים: 48 מנות חלב⇒לקטוז, 24 הוסר דגים, published_menu סונכרן בטריגר.
⚠️ ה-rebuild clone צריך `git pull` כרגיל.

## מוקשים + ה-Edge Function (2026-08-13)

- `PITFALLS` (טופס המנה, `MenuHealthReview`) — **מוקשים ≠ אלרגיות**. `דג נא` הוצא
  מרשימת האלרגנים. ר' CLAUDE.md הראשי, "מוקשים (pitfalls)", לכלל המלא.
- ⭐ `supabase/functions/menu-ai-parse/index.ts` — **עותק מקומי, חדש**. עד 2026-08-13
  הפונקציה נערכה רק דרך הדשבורד. **ערכו כאן ואז דפלוי** (MCP `deploy_edge_function`;
  ל-CLI אין access token בסביבה הזו). הפרומפט חי בקובץ הזה.
  ✅ **שער ראיות מפורשות ל-`pregnancy`** (`דג נא`/`בשר נא` דורשים סימן גלמיות; סימן
  בישול שולל, אבל **רק על הרכיב שנדגל** — רול עם סלמון נא + שבבי טמפורה שומר על הדגל)
  **נפרס** ונמצא ב-v18. ⚠️ הבאג שדווח (Gyoza מטוגן ⇒ `בשר נא`) לא שוחזר ב-15 קריאות —
  זו הקשחה מונעת, לא תיקון לבאג חי.

  ✅ **v19 פרוס (2026-08-13, סוף היום) = הקובץ המקומי, נקי.** ה-`DEV_NONCE` שהיה
  ב-v18 (דלת אחורית להרצת פרומפט שרירותי על מפתח ה-AI, שימש את לופ המדידה) **הוסר
  לפני הפריסה הסופית** — אל תחזירו אותו; אם צריך שוב איטרציות פרומפט, מדפלים גרסת
  מעבדה זמנית ומוחקים בסוף באותו סשן. שני ה-clones סונכרנו (`diff` נקי).
  `run_lab.py --system` לא יעבוד יותר בלי הדגל — זה מכוון; ההרצה הרגילה
  (`run_lab.py full ...`) עובדת מול v19 כרגיל. ר' CLAUDE.md הראשי,
  "מנגנון הייבוא נבנה מחדש עם לופ מדידה", לתוצאות (62/62) ולכללים שאסור לשבור:
  2576px/0.92, קריאה-לכל-תמונה במקביל (504 אחרת), Opus 5 בלי temperature,
  `content.find(type=text)` ולא `content[0]`.

## סבב אופטימיזציית המנהל (2026-08-13, לילה) — קבצים חדשים

```
src/components/SmartSuggestions.jsx — כרטיסי הצעה בטאב הבית (מנה חלשה/קטגוריה נכשלת ⇒ כוכב באישור)
src/components/GuidedTour.jsx       — סיור מודרך בכל הטאבים; אוטומטי אחרי ייבוא ראשון, ידני מהגדרות
```

ב-`OwnerDashboard.jsx`: `starred` (טוגל ⭐ + auto-star ל-insert בודד אחרי 48h שקט —
`isStandaloneNewDish`; ייבוא שלם לעולם לא מסמן) · `EmphasisSuggestion` בסקירת הייבוא ·
`DIFFICULTY_PROFILES` בשלב 3 של האונבורדינג (נכתב ל-exam_config רק כשאין) · תיאור גלוי
על כרטיס מנה · backdrop צהוב ל-[?] בבדיקת התמלול (הפונט/padding חייבים להיות זהים
לטקסטארה) · `openDishEditor` + useEffect שגולל את `#dish-form` לראש (container **וגם**
`window.scrollTo(0,0)` — בחלון דסקטופ הדף עצמו נגלל; ⚠️ ה-useEffect מעל ה-early return
של האונבורדינג).

✅ **צריכת `starred` במנוע המלצרים — מחוברת (2026-08-13, סשן המלצרים).** צד המלצר
קרא רק את `is_special` הישנה, ולכן הטוגל לא עשה כלום אצל הצוות; `pubToCard` קורא
עכשיו `starred || is_special`. נבדק דרך הכפתור עצמו: ⭐ בממשק המנהל ⇒ המנה ראשונה
בסבב אצל המלצר עם "המנהל סימן: זו מנה חשובה ונמכרת". ר' `shiftcrew-waiter/CLAUDE.md`,
"כוכב המנהל".

## ⚠️ TEMP DEV BYPASS — קיים בקוד אבל כבר לא נחוץ

`App.jsx` בודק `import.meta.env.VITE_DEV_BYPASS_AUTH === "true"` — אם כן, מדלג לגמרי על
login/session ומציג דשבורד עם מסעדת-דמו. כבוי כברירת מחדל, מופעל רק דרך `.env.local`
מקומי (**gitignored**). נוצר כשPostgREST היה תקוע (PGRST002) — **התקלה נפתרה**, אז זה
כבר לא נחוץ בפועל, אבל נשאר בקוד כ-escape hatch. אל תסמכו עליו קיים בסביבה אחרת.

## סטטוס (2026-08-11)

- ✅ מסונכרן עם `origin/main` (כולל commits של Eytan Shleizer) — אין push ממתין.
- ✅ נבדק חי בדפדפן: התחברות → אונבורדינג → טאב צוות מציג חבר צוות אמיתי שנוצר דרך
  ה-waiter app, עם הנתונים הנכונים מ-`leaderboard`.
- ℹ️ ל-SALON **יש** סיסמה, אבל היא שונתה מאז זו שנקבעה ב-2026-08-11 — היא לא ידועה לנו.
  לבדיקות מקומיות: הוסיפו שורה זמנית ל-`menu_app.owner_users` (ה-RPC בודק גם מולה)
  ומחקו אותה אחרי. **אל תדרסו את `owner_password_hash`** — סביר שזו הסיסמה של המשתמש.

## הרצה מקומית

```bash
npm install
npm run dev -- --host 127.0.0.1 --port 5177
```
`.env` קיים עם `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`. הוסף `.env.local` עם
`VITE_DEV_BYPASS_AUTH=true` אם רוצים לראות את הדשבורד בלי להתחבר (כבר לא נחוץ בפועל).

## סטטוס למידה + מדריך בריף (2026-08-13)

- **`LearningStatus.jsx`** — טאב "סטטוס" (7 טאבים עכשיו, `grid-cols-7`). קריאה בלבד.
  שלוש קבוצות: **למדו היום** / **נכנסו אבל לא למדו** / **צריכים ללמוד** (ממוין מהחלש).
  ⚠️ הקבוצה האמצעית היא כל הרעיון — כל מדד מבוסס-התקדמות מסתיר מלצר שנכנס כל יום
  ולא לומד. היא מתאפשרת רק בזכות `team_members.last_seen_at` (עמודה חדשה, נכתבת
  ע"י ה-waiter בכל טעינה) — **נוכחות נרשמת בנפרד מהתקדמות**.
  האחוז מחושב באותה נוסחה כמו ב-waiter ובטאב צוות (`sum/(n*5)`) — שלושה מסכים
  שחלוקים על אותו מלצר גרועים יותר ממספר לא מדויק.
- **`BriefAssistant.jsx`** — מופיע בטאב בית **רק כשהבריף של היום ריק**. שלושה שלבים:
  חוסרים ⇒ המלצות (מזהה קטגוריות שנוספו לאחרונה: "תרצו שימליצו על תפריט X החדש?")
  ⇒ הערות. **לא כותב לבד** — הכל נכנס ל-`briefDraft` של הטופס הידני והבעלים שומר.
  `dishFromDb.createdAt` נוסף בשבילו.

## ⚠️ launch.json — תוקן 2026-08-13

הקונפיג `shiftcrew-owner` (פורט 5177) הצביע על **`shiftcrew-owner-rebuild`**, לא על
התיקייה הזו — אותה מלכודת כמו `owner-app`. תוקן להצביע על `shiftcrew-owner`.
אם preview לא מציג שינוי שכתבתם — בדקו את זה **לפני** שמחפשים באג בקוד.

## 🔴 הבאג של טאב הצוות (2026-08-13) — הכי חשוב לזכור

```js
db.from("team_members").select("*").eq("restaurant_id", id)
  .order("menu_position", ...)   // ← עמודה שקיימת רק ב-menu_items
```

`menu_position` הועתקה לשאילתת `team_members` יחד עם שינוי סדר התפריט (כולל
ההערה על "סדר התפריט המודפס" — סימן מובהק להעתקה). PostgREST דוחה מיון לפי עמודה
שלא קיימת ⇒ `teamErr` תמיד מלא ⇒ `setTeamMembers` אף פעם לא נקרא ⇒ **כל טאב
"צוות" הציג "עדיין אין חברי צוות"** בכל המסעדות, כולל האחוזים, המנות החלשות,
צ'יפי המבחנים וגרף השיפור.

**למה זה שרד**: כישלון נראה זהה למסעדה חדשה בלי צוות. `if (!teamErr)` כן היה שם —
אבל בלי `console.error` ובלי מצב-שגיאה ב-UI, "נכשל" ו-"ריק" נראים אותו דבר.
**נתפס** כי טאב "סטטוס" הראה 2 חברי צוות כשטאב "צוות" הראה 0 — שני מסכים שקוראים
את אותו נתון וחלוקים עליו הם כלי האיתור הכי טוב כאן.

**הכלל**: כשמוסיפים `.order()` — לוודא שהעמודה קיימת **בטבלה הזו**. ורשימה ריקה
אחרי שגיאה חייבת להיראות אחרת מרשימה ריקה אמיתית.

## ⚠️ ארבע קבוצות אזהרה — הקוד מוכן, הנתונים לא

`pregnancy` ו-`kashrut` קיימות בסכימה, ב-`dishFromDb` וב-payload השמירה. **בנתונים
הן ריקות ב-94/94 המנות**, ו-`דג נא` עדיין ב-`pitfalls` (56 מנות). כל עוד לא רצה
מיגרציה — אל תתעדו ואל תניחו שהטקסונומיה בת ארבע הקבוצות חלה על הנתונים.

## דף הבריף היומי — מי קרא (2026-08-13)

`BriefReadBoard.jsx`, מתחת ל-`DailyBriefEditor` בטאב בית: ✓ ירוק (קרא וענה נכון) ·
✗ אדום **עם התשובה שנתן** · שעון אפור (טרם קרא). **מי שלא קרא ראשון** — רשימת מטלות.

⚠️ **למה זה נבנה**: אפליקציית המלצרים כתבה שורת "נקרא" **אוטומטית בטעינה**, ולכן
הלוח הראה ✓ לכל מי שפתח את האפליקציה. עכשיו האישור נרשם רק אחרי תשובה לשאלה
שנבנית מתוכן הבריף (`daily_brief_reads.question/answer/correct`). **תשובה שגויה
היא המקרה השימושי** — אישרו קריאה בלי לקרוא.
ℹ️ שורות ישנות עם `question = NULL` מוצגות כ-✓ בלי שורת שאלה; הן מלפני השינוי.

## ⭐ סבב מודל-המפעיל + עוזר התפריט (2026-08-13, ערב)

**מודל מפעיל**: יצירת חשבון הוסתרה מהמסך הראשי — `OwnerLogin.jsx` מציג את טאב "יצירה"
רק עם `?signup=1` ב-URL (למפעיל בלבד). בעלים חדש שנכנס עם הקוד נוחת ישר בסיור/ייבוא:
`OwnerDashboard` משלים `onboarding_completed` בשקט ופותח את מדריך הייבוא כשהתפריט ריק.

**ניווט חדש — 5 טאבים בלבד**: בית · תפריט · צוות · הגדרות · יציאה.
- טאב "פרטים" נמחק — פרטי המסעדה עברו לכרטיס בתוך "הגדרות".
- טאב "סטטוס" מוזג לתוך "צוות": מתג פנימי "מי למד היום" (LearningStatus) /
  "התקדמות ומבחנים" (הכרטיסים הקיימים). `teamView` state.
- טאב "פקודות" נמחק — `CommandsTab` מרונדר בראש טאב התפריט, **מעל** "הוסף מנה".
- `GuidedTour` עודכן ל-4 תחנות בהתאם.

**עוזר התפריט (`CommandsTab.jsx` + edge v23)** — שלושה מסלולים בתיבה אחת:
1. **פקודות שינוי** ("תוריד את כל סימני השאלה") ⇒ patches עם תצוגה מקדימה ואישור.
2. **הוספת מנות בהדבקה** ("תוסיף: פסטה פומודורו 58") ⇒ `additions` — נבדק חי; מנה
   בלי מחיר לא מומצאת אלא מוחזרת ב-warning. באישור: insert עם `menu_position` בסוף.
3. **מיני-עוזר** — שאלה ("אילו מנות חסר להן תיאור?") ⇒ שדה `answer` על סמך התפריט
   בלבד, patches ריק. הפרומפט מסרב בנימוס לשאלות שאינן על התפריט.
המודל לעולם לא כותב — הכל preview+approve. השרת מנקה הכל (רשימות סגורות, ids).

**העשרת יינות — חי ומאומת מקצה לקצה** (edge v22 תיקן חילוץ מערך JSON):
כפתור "השלם תיאורי יינות" ב-CommandsTab ⇒ זיהוי משם היין בלבד (Opus), טבלת זיכרון
משותפת `menu_app.wine_knowledge` (norm_name unique — פעם ראשונה בתשלום, אחר כך חינם),
יין לא מזוהה מוחזר בכנות "לא זוהה בוודאות". כשרות לעולם לא נקבעת (מנוקה גם בשרת).
נבדק: ירדן קברנה/דלתון שרדונה ⇒ תיאור מלא נשמר; יקב פיקטיבי ⇒ נשאר ריק. 0 הזיות.

**מסך "לגו" לקטגוריות (`CategoryComposer`)** — בסקירת הייבוא, מעל רשימת הקטגוריות:
צ'יפים לכל קטגוריה (שם + מס' מנות), בחירת 2+ ⇒ שדה שם + "איחוד N קטגוריות".
נבנה בגלל תפריטי אלכוהול שמתפצלים לקטגוריה-לכל-יין; הבעלים מחליט לאחד או להשאיר.
נבדק חי: "יינות אדומים"+"יינות לבנים" ⇒ "יינות · 4", וודקה נשארה נפרדת.

⚠️ **סנכרון ריפו↔פרוס**: מ-v23 הפונקציה הפרוסה נפרסה מתוכן הקובץ בריפו הזה
(`supabase/functions/menu-ai-parse/index.ts`) — זה המקור הסמכותי. clone ה-rebuild
מסונכרן דרך git pull, לא עריכה ידנית.

## 🔧 2026-08-14 — צ'אנקים ליינות + תור בקשות למפעיל

- **enrich_wines בצ'אנקים**: תפריט של 105 משקאות שבר את הקריאה האחת (חריגת תקציב
  פלט ⇒ "התפריט ארוך מדי"). `runWineEnrichment` ב-`CommandsTab.jsx` מפצל עכשיו
  ל-15 יינות לקריאה, מריץ במקביל עם ניסיון חוזר אחד לצ'אנק. נבדק על 35 (3 צ'אנקים):
  33 זוהו, מזויפים הוחזרו בכנות. **אל תאחדו חזרה.**
- **תור בקשות למפעיל**: טבלה חדשה `menu_app.operator_requests` + שתי דרכי שליחה
  ב-CommandsTab: קישור קבוע מתחת לתיבת הפקודה, והצעה אוטומטית כשהעוזר מחזיר
  0 שינויים (במקום מבוי סתום). ר' סעיף "תור בקשות למפעיל" ב-CLAUDE.md הראשי —
  כולל שאילתת הבדיקה שחובה להריץ בתחילת כל סשן.

## 📱 2026-08-14 — Capacitor + session tokens (ר' CLAUDE.md הראשי לתמונה המלאה)

- `capacitor.config.json` + `ios/` + `android/` (appId `il.co.shiftcrew.owner`).
  אחרי כל `npm run build` ⇒ `npx cap sync`.
- `index.html`: נוסף `viewport-fit=cover` — בלעדיו כל ה-safe-area שכבר היה בקוד
  חושב ל-0. ה-bottom nav וה-headers ב-`OwnerDashboard.jsx` קיבלו padding-insets.
- **Auth**: `OwnerLogin` עבר ל-`owner_login_v2` (טוקן נשמר דרך `lib/appSession.js`,
  נשלח כ-`x-app-session` ע"י ה-fetch המוזרק ב-`lib/supabase.js`). נוסף "שכחתי סיסמה"
  (⇒ `forgot_password_request` ⇒ תור המפעיל). יצירת חשבון עושה login v2 מיד אחרי.
- **`components/AccountSecurity.jsx`** בטאב הגדרות: החלפת סיסמה
  (`change_owner_password`) + מחיקת חשבון (`delete_restaurant_account`, הקלדת
  "מחיקה" + סיסמה). נבדק חי מקצה לקצה. דרישת חנויות.
- **`migrations/02_rls_session_cutover.sql`** — ⚠️ להפעיל רק אחרי דיפלוי שתי
  האפליקציות. `public/privacy.html` — מדיניות פרטיות ציבורית.

## 🔧 2026-08-14 (המשך) — "המנהל עושה בעצמו" תוקן

הדיווח "זה לא עובד" ⇒ שחזור מלא בפרודקשן הראה שהמנגנון עבד, אבל שלוש בעיות אמיתיות:
1. **הקישור "שלחו למפעיל" ישב צמוד לכפתור ההרצה** ונלחץ במקומו (כך נולדה בקשת
   "פסטה פומודורו" בתור). הוסר מהמסלול הראשי — מופיע רק כשהעוזר מחזיר 0 שינויים
   או כשהריצה נכשלה. **הרצה היא הפעולה היחידה מתחת לתיבה.**
2. **תפריט גדול (>120 מנות) ⇒ sonnet** — נמדד על שילה (245 מנות): פקודה המונית
   ("תוריד את כל ה-[?]") עם haiku תיקנה 8/23; sonnet 22/23 (~100 שניות). אל תחזירו
   ל-haiku בתפריטים גדולים.
3. **חיווי המתנה** — בתפריט גדול הריצה לוקחת עד דקה; הכפתור אומר את זה עכשיו.

## 🔄 2026-08-15 (ערב) — סנכרון בין המכונות הושלם + רי-ברנדינג CrewMenu

- המק החדש משך את `3870e6d` (אייקוני המותג, אינדיגו) מ-origin/main. קונפליקט יחיד
  ב-`index.html` נפתר: favicon החדש + title החדש ("CrewMenu — ניהול") נשמרו יחד.
- **רי-ברנדינג**: appId → `il.co.crewmenu.owner`, שם תצוגה "CrewMenu ניהול". השם
  הקובע מעתה: **CrewMenu**.
- `Info.plist`: נוסף `ITSAppUsesNonExemptEncryption=false`.
- ⚠️ ה-RLS session-token cutover חי בפרודקשן — לא נוגעים ב-policies של `menu_app`.

## 🍏 2026-08-17 — iOS builds הועלו ל-App Store Connect מ-homestation

**הקשר**: המק השני שודרג ל-macOS 27 עם Xcode בטא בלבד, ואפל דוחה builds מ-Xcode
בטא ⇒ צד ה-iOS עבר ל-homestation (Xcode 26.6 יציב, build 17F113).

**בוצע**: שתי האפליקציות (owner + waiter) — גרסה 1.0, **build 6** — נבנו והועלו
בהצלחה. build 5 כבר היה תפוס בשרת (הועלה בעבר מהמק השני לפני שנפסל).

**הצינור שעבד (לכל אפליקציה):**
```
npm run build && npx cap sync ios          # בשורש הריפו
cd ios/App
xcodebuild -project App.xcodeproj -scheme App -configuration Release \
  -destination "generic/platform=iOS" -archivePath <path>.xcarchive \
  archive -allowProvisioningUpdates
xcodebuild -exportArchive -archivePath <path>.xcarchive \
  -exportOptionsPlist ExportOptions.plist -exportPath <out> -allowProvisioningUpdates
```
`ExportOptions.plist`: `method=app-store-connect` · `destination=upload` ·
`teamID=Q9994667T8` · `uploadSymbols=true` · `manageAppVersionAndBuildNumber=false`.

**אימות**: אין מפתח API בשום מקום (ה-`AuthKey_*.p8` שהובטח מעולם לא הגיע ל-git) —
ההעלאה עובדת דרך חשבון האפל המחובר ב-Xcode של homestation (Yotam Ezer, Team
`Q9994667T8`) עם cloud signing. מכונה אחרת שתריץ את הצינור צריכה או Xcode מחובר
לחשבון בעל הרשאה ב-Team, או מפתח API אמיתי.

⚠️ **מספר build חייב לעלות בכל העלאה**: `xcrun agvtool new-version -all <n>`
בתוך `ios/App`, ואז **לשחזר** את `Info.plist` (`git checkout`) — agvtool דורס את
`$(CURRENT_PROJECT_VERSION)` בערך קשיח, וה-pbxproj לבדו מספיק. נכון לעכשיו: **6**
בשני הריפואים (committed).

## 📱 2026-08-20 — תצוגת מלצר (WaiterPreview, טרם committed)

`src/components/WaiterPreview.jsx` — כפתור "תצוגת מלצר" קבוע ב-header של הדשבורד
(בכל הטאבים): overlay מסך-מלא עם iframe של אפליקציית הצוות בתוך מסגרת טלפון.
ב-dev מצביע על `localhost:5176`, בפרודקשן על `shiftcrew-waiter.vercel.app` —
זו האפליקציה האמיתית, לא עותק, אז אין דריפט. המנהל נכנס בתוכה עם קוד הצוות שלו
(ה-session חי בתוך ה-iframe, לא נוגע ב-session של המנהל). רענון = remount של
ה-iframe (key), כי cross-origin לא מאפשר reload פנימי.

## 🧭 2026-08-20 (ערב) — יציאה בפינה · פירוק טאב הצוות · טאב משימות · בית מקוצר

ארבע בקשות של המשתמש בסבב אחד. **נבדקו חי מול CREWDEMO** (שרת `--mode qa` זמני שעקף
את `VITE_DEV_BYPASS_AUTH`, נמחק בסוף).

### 1. `SignOutButton.jsx` — יציאה עברה מהניווט התחתון לפינה הימנית העליונה

הכפתור היה סלוט אחד מ"הגדרות" בניווט התחתון — מקש שגוי בטלפון החזיר את המנהל למסך
הכניסה באמצע עבודה. עכשיו: אייקון בפינה (ראשון ב-flow של RTL), ואחריו מודאל שבו כפתור
האישור **מושבת 5 שניות** עם ספירה לאחור. אומת: לחיצה בזמן הספירה לא מנתקת; אחרי 5
שניות הכפתור נצבע אדום ופעיל.
ℹ️ **אותו רכיב ואותו ניסוח כמו `SignOutButton` באפליקציית המלצרים** (`MainApp.jsx`) —
שתי אפליקציות ששואלות את אותה שאלה צריכות לשאול אותה אותו דבר. שינוי כאן ⇒ לשקול שם.

### 2. טאב "צוות" פורק — הניווט ירד ל-4 טאבים

**בית · תפריט · משימות · הגדרות** (`grid-cols-4`). הטאב החזיק שני דברים לא קשורים:
מספרים שנבדקים כל יום, וקוד הצטרפות שנוגעים בו פעם. הם הופרדו:
- **הנתונים → מסך הבית** — `LearningStatus` ו-`TeamProgressList` (הקומפוננטה החדשה
  שאליה חולצו כרטיסי המלצרים, בתחתית `OwnerDashboard.jsx`).
- **הקוד והרשימה → הגדרות** — `TeamRoster.jsx` (קוד + העתקה + רשימת חברי הצוות עם
  תאריך הצטרפות + הסרה בשני שלבים). ⚠️ ההסרה מסתמכת על ה-CASCADE שנוסף ל-`menu_progress`
  ו-`leaderboard` ב-2026-08-12; השגיאה מוצגת ולא נבלעת, כדי ש-CASCADE חסר עתידי ייראה.

### 3. `TasksManager.jsx` — טאב "משימות" (חדש)

ניהול `menu_app.shift_tasks` שאפליקציית הצוות כבר קוראת. **ספרייה של 47 משימות מוכנות**
בארבע קבוצות: צ׳קליסט פתיחה (14) · במהלך המשמרת (12) · צ׳קליסט סגירה (13) · לימוד
ותפריט (8). המנהל **בוחר ומאשר, לא מחבר** — טופס חופשי קיים אבל הוא לא המסלול הראשי.
לכל משימה: עריכה (כותרת+פירוט), הסתרה זמנית (👁 `active`), סידור מחדש (↑↓), מחיקה,
וכמה עובדים סימנו אותה היום (מ-`shift_task_done`).

⚠️ **סדר הקבוצות הוא סדר המשמרת, וזה נאכף ב-`position`**: אפליקציית הצוות ממיינת לפי
`position` בלבד, ולכן **כל שינוי מריץ מספור מחדש על כל הרשימה** (פתיחה → משמרת →
סגירה → לימוד) ב-upsert אחד. בלי זה משימת סגירה שנוספה ביום שלישי מופיעה מעל צ׳קליסט
הפתיחה ביום רביעי. אומת ב-DB: פתיחה 1-2, סגירה 3, וסידור ידני בתוך קבוצה שרד.

⚠️ **המסך לעולם לא כותב `kind='learning'`** — הצוות מדלג על הערך הזה כי הוא גוזר את
המשימות האלה מהתקדמות אמיתית. במקום זה יש בלוק **קריאה בלבד** (`AUTOMATIC`) שמראה
למנהל מה האפליקציה סוגרת לבד (עדכון יומי, מנות חדשות, יעד דקות). קבוצת `other` אוספת
`kind` לא מוכר (למשל שורות `learning` ישנות) כדי ששום שורה לא תהפוך לבלתי-נראית.

🔴 **מיגרציה שנדרשה**: `shift_tasks_kind_check` הרשה **רק** `'shift'`/`'learning'`,
ולכן כל שמירה נכשלה. מיגרציה `shift_tasks_kind_add_shift_phases` הרחיבה ל-
`shift/learning/opening/closing/training`. **תוספת בלבד** — המשמעות של שני הערכים
הישנים לא השתנתה, ולכן צד המלצר לא הושפע.
ℹ️ נתפס רק כי כשל ההוספה מוצג ב-UI ומודפס עם `error.message/details/hint/code`.
**אל תדפיסו `error` כאובייקט** — הקונסולה מציגה `[object Object]` וזה חסר תועלת.

### 4. מסך הבית קוצר

- באנר "ברוכים הבאים" (כרטיס שלם עם כפתור לטאב אחר) הוחלף ב**שלוש אריחי מספר** לחיצים:
  מנות · חברי צוות · משימות פעילות.
- ⚠️ **`BriefAssistant` ו-`DailyBriefEditor` רונדרו שניהם, מוערמים** — זה היה הגורם
  הכי גדול לאורך. עכשיו `showBriefAssistant` בוחר ביניהם; הכפתור "אכתוב לבד" של
  האסיסטנט הוא בדיוק המתג.
- שלושת מסכי המעקב (מי קרא · מי למד היום · התקדמות ומבחנים) עברו ל-`SettingsSection`
  **מקופל, אחד פתוח בכל רגע**, וכל כותרת נושאת את התשובה שלה ("0 מתוך 17 למדו היום",
  "ידע ממוצע 25% · הכי זקוק/ה לתרגול: X"). בונוס: הם שולחים שאילתות רק כשנפתחים.

### 5. `GuidedTour` עודכן

התחנות של `tab: "team"` הצביעו על טאב שנמחק (הסיור היה מנווט לשומקום). עכשיו:
מעקב → `home`, הוספת חברי צוות → `settings`, ותחנה חדשה ל-`tasks`.
⚠️ **סיור שמנווט לפי שם טאב חייב להתעדכן עם כל שינוי ניווט** — אין לו הגנה מפני
שם שלא קיים.

## 🔎 2026-08-20 (לילה) — שורה אחת לעובד + המשימות ירדו לדף הבית

המשך ישיר לסבב הקודם, אחרי שהמשתמש ראה אותו: **"אם יש לי 50 עובדים אני בחיים לא
אצליח לעקוב."** ארבעה שינויים, כולם באותו כיוון — רשימות סורקות, פרטים בלחיצה.

### `MemberSheet.jsx` — כל הפרטים על עובד, במקום אחד

שתי הרשימות בבית היו כרטיס מלא לכל אדם (גרף שיפור, המנות שהוא טועה בהן, צ׳יפי מבחנים,
שני פילים). זה קריא לשלושה מלצרים ובלתי-אפשרי לחמישים. עכשיו כל רשימה היא **שורה
אחת לאדם** (`MemberRow`, מיוצא מאותו קובץ — שם + אחוז + פס דק + נקודת סטטוס), ולחיצה
פותחת גיליון תחתון עם הכל.

⚠️ **גיליון אחד משרת את שתי הרשימות בכוונה.** שתי תצוגות פירוט לאותו עובד היו נסחפות
זו מזו, והמנהל היה צריך ללמוד איזה מסך אומר את האמת.
⚠️ **`LearningStatus` מעביר את השורות שלו למעלה** (`onRows`) כי רק הוא מחשב את דקות
הלימוד של היום ואת עמודות השבוע. בלי זה, לחיצה על אותו עובד מ"התקדמות ומבחנים" הייתה
מחזירה גיליון **חסר** לעומת לחיצה מ"מי למד היום" — נתפס בבדיקה חיה.

### סדר ומצב פתיחה

- **"מי למד היום" פתוח כברירת מחדל** (בקשת המשתמש) — זו השאלה שפותחים איתה את האפליקציה.
- סדר הכרטיסים בבית: **מי למד היום ⇒ התקדמות ומבחנים ⇒ משימות המשמרת ⇒ מי קרא את העדכון**.
- בתוך "מי למד היום" הסדר נשאר **קודם מי שלמד, אחריו מי שנכנס ולא למד, ואחרון מי שלא
  נכנס** — ביום תקין החדשות הטובות הן הרשימה הקצרה, והפער בולט.
- "התקדמות ומבחנים" ממוין **מהחלש לחזק** — זו רשימת מטלות, לא טבלת דירוג.

### טאב המשימות ירד לדף הבית — הניווט ל-3 טאבים

**בית · תפריט · הגדרות**. המשימות מוגדרות פעם אחת ואז כמעט לא נוגעים בהן, אז הן לא
הצדיקו סלוט קבוע בניווט. ⚠️ **`GuidedTour` עודכן שוב** — התחנה `tab: "tasks"` הצביעה
על טאב שנמחק. סיור שמנווט לפי שם טאב חייב להתעדכן עם כל שינוי ניווט.

**שילוב המשימות בתוך המסכים הקיימים** (במקום מסך נפרד): כמה משימות כל עובד סימן היום
מופיע **בשורה שלו** ב"התקדמות ומבחנים" (`משימות 3/7`) **ובגיליון הפרטים** שלו. הנתון
מגיע מ-`shift_task_done` בטעינת הדשבורד — ה-RLS מרשה לבעלים לקרוא את כל השורות של
המשימות של המסעדה שלו (ענף ה-EXISTS ב-policy).

ℹ️ ריבוי בעברית תוקן בכמה מקומות ("הוספת 1 משימות" ⇒ "הוספת משימה אחת").

## 📣 2026-08-20 (מאוחר) — סדר חדש בבית · המשימות של המנהל · הודעה למלצר

### הסדר שהמשתמש ביקש

**העדכון היומי (+מי קרא) ⇒ משימות המשמרת ⇒ מי למד היום ⇒ התקדמות ומבחנים.**
שני הראשונים הם מה שהמנהל **כותב** בבוקר, שני האחרונים מה שהוא **בודק** אחר כך —
ולכן חצי-הכתיבה למעלה. "מי קרא את העדכון" הפסיק להיות כרטיס נפרד ונכנס לתוך כרטיס
העדכון: כתיבה ובדיקה של אותו דבר הן עבודה אחת.

### `OwnerDayTasks.jsx` — המשימות של המנהל עצמו

בקשת המשתמש: **"בתחילת היום זה ממש יופיע לו שני הדברים האלה למלא כמו משימות עבורו."**
כרטיס ראשון בבית, באותה צורה של רשימת המשימות של המלצר: ✓/! · כותרת · תת-כותרת ·
כפתור שקופץ ישר לדבר עצמו. שתי שורות בלבד — לכתוב את העדכון היומי, ולהגדיר את
משימות המשמרת — עם מונה `1/2`. המלצרים פותחים את האפליקציה לרשימה ממוספרת של מה
שהיום דורש מהם; המנהל פתח אותה לקיר של מספרים של אנשים אחרים.

⚠️ **הכרטיס שנפתח כברירת מחדל תלוי בשעה ביום**: אין בריף היום ⇒ "העדכון היומי" פתוח;
יש ⇒ "מי למד היום". ⚠️ **הדגל הוא `loadDone` ולא `menuLoaded`** — `menuLoaded` נדלק
מיד אחרי השאילתה **הראשונה** ב-effect הטעינה, בזמן שהבריף נשלף כמה שאילתות אחר כך,
ולכן קריאת `briefSent` באותו רגע תמיד אומרת "אין בריף" ופותחת את הכרטיס הלא נכון.
⚠️ ו-`useRef` שומר שזה יקרה **פעם אחת** — אחרת שמירת הבריף הייתה סוגרת את הכרטיס
מתחת לידיים של המנהל.

### `TeamMessageDialog.jsx` + טבלת `menu_app.team_messages`

כפתור ✉ **רק ליד מי שלא למד היום** (בקבוצות "נכנסו ולא למדו" ו"צריכים ללמוד"), וגם
בגיליון הפרטים. נוסחים מוכנים קודם, טקסט חופשי אחריו — אותו כלל כמו ספריית המשימות:
מנהל שמסתכל על תיבה ריקה ב-11:00 לא כותב כלום.
- **הטבלה**: `restaurant_id`, `team_member_id`, `body`, `created_at`, `read_at`.
  RLS: הבעלים מגיע לכל ההודעות של המסעדה שלו; מלצר **רק לשלו** (`session_member()`),
  וזה מה ששומר הודעה אישית אישית. ✅ אומת ש-`relrowsecurity` דלוק, לא רק שה-policy קיים.
- **כבר נשלחה הודעה היום** ⇒ הכפתור ירוק והדיאלוג מציג את מה שנשלח, כדי שלא יישלחו
  שלוש תזכורות לפני הצהריים.

🚧 **צד המלצר עדיין לא מציג את ההודעות** — אין ב-`shiftcrew-waiter` שום מסך שקורא
את `team_messages`. עד שייבנה שם, ההודעה יושבת ב-DB. לכן יש בדיאלוג גם **"העתקה
לשליחה בוואטסאפ"** — ערוץ שעובד היום. **הספק לצד המלצר**: לשלוף
`team_messages` של `session_member()` שבהן `read_at is null`, להציג בראש מסך
המשימות, ולעדכן `read_at` בקריאה.
ℹ️ נשקל קישור `wa.me` ישיר ונפסל: **אין טלפון ב-`team_members`**.

## 👋 2026-08-20 (סוף) — ברכה לפי שעה · מה שטופל יורד למטה · צ׳קליסט שבועי וחודשי

### `Greeting.jsx`

"בוקר טוב, דנה" בראש מסך הבית + התאריך המלא. הברכה נגזרת מהשעה: 5-11 בוקר ·
12-15 צהריים · 16-17 אחר צהריים · 18-21 ערב · אחרת לילה. השם הוא
`logged_in_as_name` (מנהל משני) ואם אין — שם המסעדה.
⚠️ `truncate` על השורה: שמות מסעדה ארוכים ("מסעדת הדגמה — CrewMenu") גלשו לשתי
שורות, וברכה שנשברת מפסיקה להיראות כברכה.

### מה שטופל יורד לתחתית העמוד

⚠️ הגרסה הראשונה של `OwnerDayTasks` השאירה את שתי השורות במקום וצבעה אותן ירוק,
כלומר ב-10:00 ראש המסך היה שני ✓ שצריך לגלול מעליהם בכל כניסה (בקשת המשתמש:
"שזה לא יהיה תקוע למעלה בירוק"). עכשיו:
- **הכרטיס העליון מציג רק את מה שלא נעשה.** שניהם נעשו ⇒ הוא לא מרונדר בכלל.
- **`OwnerDayDone`** — שורות דקות ומעומעמות בתחתית הבית ("טופל היום"), כל אחת עם
  "עריכה" שפותחת את הכרטיס המתאים.
- שניהם קוראים את **אותו** `buildRows` דרך `dayTaskProps` בדשבורד, כדי שהראש
  והתחתית לא יחלקו על מה גמור.

### צ׳קליסט שבועי וחודשי — "קבוע לתמיד"

בקשת המשתמש: צ׳קליסט שבועי/חודשי שתמיד יהיה שם. **אותה טבלה, אותו UI** — רק
`kind` חדש (`weekly`, `monthly`, מיגרציה `shift_tasks_kind_add_weekly_monthly`).
צ׳קליסט הוא צ׳קליסט; מה ששונה הוא רק התקופה שבה הסימון מתאפס.
- ספריות מוכנות: 10 שבועיות (ניקיון עומק, ספירת אלכוהול, מעבר על ציוני המבחנים…)
  ו-10 חודשיות (ספירת מלאי, רישיון עסק, בטיחות, מבחן תפריט מלא…).
- **הספירה היא לפי תקופה ולא לפי היום**: שאילתה אחת אחורה עד תחילת החודש, ומחלקים
  ל-day/week/month. משימה שבועית שסומנה ביום שני היא "בוצעה השבוע" גם ביום חמישי —
  ספירה לפי `done_date = today` הייתה מציגה אותה כלא-בוצעה שישה ימים.
  ⚠️ נספרים **חברי צוות ייחודיים** לתקופה, לא סימונים.
- **תחילת שבוע = יום ראשון**, כמו `weekly_scores` בצד המלצר. גבול שבוע אחד למוצר.

🚧 **צד המלצר עדיין מתייחס לכל המשימות כיומיות** — `useShiftTasks` מסנן
`done_date = today`, ולכן משימה שבועית תיראה לו לא-מסומנת למחרת. **הספק שם**:
לחשב את תחילת התקופה לפי `kind` (weekly ⇒ יום ראשון, monthly ⇒ ה-1 בחודש) במקום
`today`. הצד של המנהל כבר מדווח נכון.

### 🧹 פישוט הצ׳קליסטים (אותו יום, בקשת המשתמש: "נראה ארוך ומסובך")

שתי הבעיות היו נפרדות — ספרייה ארוכה, ואז ניהול כבד:

1. **לחיצה אחת במקום 14 תיבות.** בראש גיליון הספרייה יש עכשיו
   **"הוספת הצ׳קליסט המומלץ (6) — לחיצה אחת"**. הרשימה מציגה רק את הליבה, והשאר
   מאחורי "עוד N אפשרויות ↓". ⚠️ **`CORE_COUNT` לוקח את N הראשונים של כל מערך
   ב-`LIBRARY`** — הוספת פריט נישתי בראש מקדמת אותו בשקט לסט ההתחלה של כל מסעדה.
   פתיחה 14⇒6 בליבה, סגירה 13⇒6 (13 המקוריות אוחדו ל-11: נעילה+מקררים+כיבוי הפכו
   לשורה אחת).
2. **שורה אחת למשימה.** כל שורה נשאה ↑ ↓ 👁 🗑 + תת-כותרת, כלומר צ׳קליסט של 14
   נקרא כלוח בקרה. עכשיו: מספר + כותרת, והפקדים **בתוך** השורה — הקשה פותחת עריכה
   עם שורת פעולות קומפקטית. זה גם מה שגרם לזה להיראות כמו רשימה במקום כמו טופס.

### 📬 "נשלחה" מול "נקראה" (אחרי שצד המלצר נכנס)

סשן המלצר מימש את הצגת ההודעות (`ManagerMessages.jsx`) וכתיבת `read_at` בכפתור
"קראתי", אז לכפתור ✉ יש עכשיו **שלושה** מצבים ולא שניים:
אפור (לא נשלחה) · **כתום (נשלחה, טרם נקראה)** · ירוק עם ✓ (נקראה).
אותה הבחנה בדיאלוג עצמו. "נשלח" ו"הגיע" הן שתי עובדות שונות, ומנהל שרואה רק את
הראשונה לא יודע אם התזכורת עשתה משהו.

🔴 **באג שנתפס רק בבדיקה חיה**: `LearningStatus` העביר `messaged={!!messagedToday?.[r.id]}` —
ה-`!!` הפך את הרשומה ל-`true`, ולכן `messaged.readAt` היה `undefined` ו**כל** הודעה
נקראה כ"טרם נקראה". ה-build היה ירוק. **אל תמירו אובייקט ל-boolean בדרך לרכיב שצריך
שדה ממנו.**

## 🔴 2026-08-20 (לילה) — "שיניתי וזה לא נשמר": הכפתור, לא המנגנון

המשתמש דיווח שהוא כיבה שאלות מחיר וזה לא נשמר ולא עבר למלצרים. **המנגנון היה תקין;
כפתור השמירה היה בתחתית פאנל ארוך מאוד ופשוט לא נראה** ("לא רואים את הכפתור שמירה").

### מה אומת בפועל (לא מקריאת קוד)

הרצתי את **מנוע השאלות האמיתי של המלצרים** (`questionEngine.js`) על תפריט אמיתי,
40 דקים × 20 שאלות לכל קונפיג:

| בחירת הבעלים | הרכב השאלות בפועל |
|---|---|
| אלרגנים בלבד | allergens **100%** |
| אלרגנים > מרכיבים | allergens 65% · ingredients 35% |
| מרכיבים > אלרגנים | ingredients 60% · allergens 40% |
| כיבוי "מחיר" | price **0%** |
| בלי קונפיג (ברירת מחדל) | allergens 50% · ingredients 35% · price **15%** |

כלומר `exam_config.facets` באמת קובע **גם מה נשאל וגם כמה** — הדירוג הוא משקל
(`buildWeightedDeck`: דרגה 1 מתוך N מקבלת משקל N), וכיבוי הוא אפס מוחלט.

### מה תוקן

**סרגל שמירה צף** ב-`LearningPathSettings`: ברגע שמשנים משהו מופיע פס מעל הניווט
התחתון — "יש שינויים שלא נשמרו · הצוות יראה אותם רק אחרי שמירה" + כפתור שמירה.
אחרי שמירה: "נשמר — הצוות מתעדכן מיד", ונעלם אחרי 3 שניות.

⚠️ **`fixed` ולא `sticky`**: כרטיסי ההגדרות עטופים ב-`overflow-hidden` (בשביל הפינות
המעוגלות), ו-**ancestor עם overflow-hidden מבטל `position: sticky` בשקט** — הסרגל
פשוט לא הופיע. נתפס בבדיקה חיה, לא ב-build.

⚠️ **`dirty` נגזר מהשוואה למה שיש ב-DB** (`snapshot()` מול `persisted`), לא מדגל
שמישהו שוכח לאפס. כיבוי והדלקה חזרה של אותו מתג מדווחים נכון "אין מה לשמור".

### 🧹 הצ׳קליסטים: רשת אריחים במקום אקורדיון מקונן

שש קבוצות כאקורדיון **בתוך** האקורדיון של מסך הבית = שתי רמות קיפול ושש כותרות
דו-שורתיות לפני שרואים תוכן ("ארוך ולא מסודר"). עכשיו:
- **סקירה** — שש אריחים ב-2 עמודות, בשתי קבוצות מסומנות: **כל יום** (פתיחה · משמרת ·
  סגירה · לימוד) ו**חוזר מעצמו** (שבועי · חודשי). כל אריח: אייקון, שם, ומספר משימות
  או "להגדרה". נכנס למסך אחד.
- **לחיצה על אריח** ⇒ אותו צ׳קליסט **לבדו**, עם חץ חזרה. `groupView` החליף את `openGroup`.

### ✂️ "קו ישיר אלינו" קוצר

היה כותרת + פסקה בת שלוש שורות + ארבעה צ׳יפים של דוגמאות + כפתור קבוע — פסקת שיווק
מעל התפריט שהבעלים בא לראות. עכשיו: שורה אחת, שדה טקסט, וכפתור שמופיע **רק כשיש
טקסט**. הדוגמה היחידה ששרדה היא ה-placeholder. הבקשות הסגורות ירדו מהתצוגה — מוצגות
רק בקשות פתוחות, בשורה אחת כל אחת.

## ✅ 2026-08-21 — מסך הבית של המנהל הוא רשימת משימות (כמו במלצרים)

בקשת המשתמש מול צילום מסך: **"מוצף ולא ברור... בדיוק כמו במלצרים אני רוצה שיהיה
פיזית משימות"** — מילוי העדכון היומי, משימות פתיחה, משימות סגירה — **מחולק ל-1. יומיות
2. תפריט**, "תראה איך זה במלצרים ותיקח השראה".

### `OwnerTasksList.jsx` — הועתקו הכללים, לא רק המראה

הקומפוננטה בנויה על `TasksTab` של המלצרים, וחשוב יותר — על **הכללים** שלה:
- **כל שורה פותחת משהו.** לחיצה מובילה לעורך הבריף / לצ׳קליסט / לבדיקת התפריט,
  לעולם לא מסמנת תיבה. תיבה בלי מסך מאחוריה מודדת הקשה, לא עבודה.
- **משימה בלי תוכן לא קיימת.** אין מנות בלי תיאור ⇒ אין שורה. יום נקי הוא רשימה
  קצרה, לא קיר של ✓ ירוקים.
- **שתי קבוצות**: `משימות היום` (העדכון + פתיחה + סגירה + כללי שירות) ו`משימות
  התפריט` (מה חסר בתפריט). לכל קבוצה מונה `X/Y` ופס התקדמות משלה.
- **המספר הוא המקום בתור עכשיו**, מחושב מחדש בכל רנדר — סוגרים את #2 והישן #3
  נעשה #2, בלי חורים. שורה שבוצעה יורדת מתחת למפריד "בוצע · N".

### מה ירד מהבית ולאן

| היה | עכשיו |
|---|---|
| `OwnerDayTasks` + `OwnerDayDone` (כרטיס עליון + "טופל היום") | **נמחקו** — הרשימה מחליפה את שניהם |
| שלוש אריחי מספר (מנות/צוות/משימות) | נמחקו — מספרים בלי פעולה |
| `SmartSuggestions` על הבית | עבר ל-`TeamScreen` — הן נגזרות מביצועי הצוות |
| ארבעה כרטיסים מקופלים (מי למד · התקדמות · משימות · מי קרא) | **`TeamScreen.jsx`** מאחורי כפתור 📊 ב-header |

⚠️ **כפתור 📊 הוא בדיוק המהלך של אפליקציית המלצרים** (`MetricsScreen`): "מה לעשות
היום" ו"איך אנחנו עומדים" הן שתי שאלות שונות, וערבוב שלהן הוא מה שהפך את הבית לקיר.

### מבנה התצוגות

`homeView`: `null` (הרשימה) · `"brief"` (עורך הבריף + מי קרא) · `"checklist:<kind>"`.
`TasksManager` קיבל `initialGroup`/`onExit` — שורה שכתוב בה "משימות פתיחת משמרת"
**נוחתת על הצ׳קליסט הזה**, לא על רשת שממנה צריך לנווט. `ViewHeader` נותן לכולן
אותו חץ חזרה.

⚠️ `reloadTaskCounts()` נקרא ביציאה מהעורך — הרשימה סופרת מ-state שנטען פעם אחת,
ובלעדיו השורה הייתה ממשיכה לומר "להגדרה" מעל צ׳קליסט שכבר קיים. אומת חי: 0/4 ⇒ 1/4.

🚧 **מה שעוד לא נבנה מהבקשה**: משימות פתיחה/סגירה **מחזוריות** ("משימות קבועות +
משימה יומית שמתחלפת כל יום בחודש, ואז מחזור, והמנהל רק מאשר"). כרגע צ׳קליסט הוא
קבוע — `done` של השורה = "הוגדר", לא "אושר להיום". המימוש דורש עמודה חדשה
(`rotating`) + בחירת משימת היום, **ותיאום עם צד המלצר** כי הוא זה שמרנדר את הרשימה.

## ✍️ 2026-08-22 — הבריף בהקלדה חופשית · משימות היום-בלבד · דחייה ו-↺ ברשימת המנהל

חמש בקשות משתמש בסבב אחד, כולן סביב אותו עיקרון: פחות לבחור מתוך קירות, יותר להקליד
ולהמשיך. נבדק חי מול CREWDEMO ונוקה.

### 1. `TagField` (מיוצא מ-`BriefAssistant.jsx`) — הקלדה עם השלמה, בכל הבריף

"בחרו מנות שאזלו" הציג את **כל התפריט** כצ'יפים — עשרים כפתורים לפני שהמנהל עשה
משהו ("its too long and confusing"). עכשיו שדה הקלדה אחד, בכל שדות הרשימה של הבריף
(אסיסטנט **וגם** העורך הידני `DailyBriefEditor`):
- הקלדה מסננת את התפריט; הקשה על הצעה משלימה את שם המנה המדויק.
- מה שאינו מנה ("מלפפונים") נוסף בדיוק כפי שהוקלד, דרך ⊕ "הוספת ״X״".
- ⚠️ **טקסט שנשאר בתיבה בזמן מעבר/שמירה נשמר בכל זאת** — הכלל של המשתמש מילה
  במילה. באסיסטנט: `flush()` בכל `commit`; בעורך: `saveAll()` בונה את הדראפט הסופי
  **ומעביר אותו כפרמטר** ל-`handleSaveBrief(draftOverride)` — קריאת state מיד אחרי
  setState הייתה שומרת את הדראפט הישן.
- אומת ב-DB: מנה שהושלמה + טקסט חופשי + **שני** טקסטים תלויים ⇒ כולם נשמרו.

### 2. שמירת בריף סוגרת את המעגל

שמירה ⇒ `setHomeView(null)` בתוך `handleSaveBrief` — חוזרים לרשימת המשימות, והשורה
כבר ירוקה. ⚠️ **כל השורות הגמורות יורדות לתחתית המסך**, קטנות ו-opacity-60, תחת
מפריד "בוצע · N" (המשתמש צירף צילום: ארבעה ✓ גדולים ישבו למעלה ודחפו את משימות
התפריט הפתוחות מתחת לקפל). קבוצה שהושלמה כולה מציגה כותרת עם "4/4 ✓" ופס מלא בלבד.

### 3. ↺ — "להחזיר לאפור" לאותו יום

`done` הוא עובדה נגזרת (יש בריף, יש צ'קליסט), אז החזרה חיה כ-override:
`redoIds` ב-localStorage עם מפתח יומי (`menu-app-owner-redo-<rid>-<date>`) — מחר
מתאפס מעצמו. שמירת בריף / יציאה מעריכת צ'קליסט מנקות את הדגל של אותה משימה.

### 4. משימת צוות "להיום בלבד" + עמודת `expires_on`

בטופס "משימה משלכם": צ'יפים **קבועה — כל יום** (ברירת מחדל) / **להיום בלבד**
(`expires_on = today`, מיגרציה `shift_tasks_expires_on`). שורות שפגו **לא נמחקות** —
מסוננות בקריאה (היסטוריה + FK של shift_task_done נשמרים). Badge כתום על השורה.
מוסתר בשבועי/חודשי. אומת כולל סימולציית "מחר" (expires_on=אתמול ⇒ נעלמת מהכל).
🚧 **צד המלצר חייב את אותו סינון** ב-`useShiftTasks` — נשלחה הודעה לסשן המלצרים;
עד אז משימת יום-בלבד תופיע אצלם גם מחר.
ℹ️ משימות מותאמות רגילות תמיד נשמרו למחר (שורות קבועות) — לא היה באג, רק אי-ודאות.
עריכת ניסוח קיימת מאז ומעולם בהקשה על שורה — עכשיו יש ✏️ שאומר את זה.

### 5. דחיית משימת תפריט — "בסדר לי שזה ככה בינתיים"

✕ קטן ליד משימות התפריט ("6 מנות בלי אלרגנים") ⇒ נעלמת מהבית ל-**14 יום** ואז
חוזרת. נשמר ב-`restaurants.dismissed_menu_tasks` (jsonb, id⇒תאריך-תפוגה) — ב-DB
ולא ב-localStorage, לפי כלל ההתמדה של המשתמש. `MenuHealthReview` ממשיך להציג הכל.

### 6. משקאות פטורים מ"חסרים אלרגנים"

לקולה אין אלרגנים וזה לא פער ("you can think coca cola dosent have an alergen").
`needsAllergens()`: קטגוריות שתייה (שתי/משקא/יין/יינות/קפה/מיץ/קולה/וויסקי…) פטורות,
**חוץ מבירה וקוקטיילים** שכן נבחנים. ⚠️ שתי מלכודות עברית שנתפסו בבדיקת node:
"יינות" לא מכיל "יין" (ן סופית!) — נוסף במפורש; "בירות" לא מכיל "בירה" — נוסף.
**כשמוסיפים מילת מפתח עברית לרגקס — לבדוק גם את צורת הרבים.**

### 🔴 שתי תקלות הרשאה שנתפסו רק בבדיקה חיה (אותה שורש-סיבה)

`restaurants` מוגנת ב-**GRANTS ברמת עמודה** (זה מה ששומר על owner_password_hash),
ולכן עמודה חדשה צריכה GRANT משלה — לכל פעולה בנפרד:
1. UPDATE חסר ⇒ הדחייה "עבדה" ב-UI (עדכון אופטימי) ולא נכתבה. נתפס כי ה-error
   מודפס וה-DB אומת.
2. **SELECT חסר ⇒ כל רענון ניתק את המנהל**: הוספת העמודה ל-`RESTAURANT_COLUMNS`
   הפילה את שאילתת שחזור-הסשן ב-permission denied, ו-App.jsx מפרש כישלון כסשן פג.
   בפרודקשן זה היה מנתק את כולם. **עמודה חדשה ב-restaurants ⇒ שני GRANTS (select+
   update) לפני ה-deploy, ולבדוק רענון אחרי לוגין.**

## 🧽 2026-08-22 — חמשת הפישוטים למבקר ראשון + סריקת עברית כפולה

המשתמש אישר את כל חמש ההצעות ("do everything") + "check the whole app for grammar
mistakes. all of it twice". הכל נבדק חי מול CREWDEMO ומול מסעדת-דמו ריקה.

### 1. `GuidedTour` — שוכתב כולו מול המסכים של היום

⚠️ הסיור תיאר מסכים שלא קיימים (עורך בריף על הבית הישן, טאב משימות שנמחק). נכתב
מחדש: 7 תחנות — רשימת המשימות ⇒ העדכון היומי ⇒ הצ׳קליסטים ⇒ 📊 ⇒ תפריט ⇒ הגדרות ⇒
המטרה (מבחנים). **נבדק צעד-צעד חי**: כל תחנה מנווטת לטאב הנכון. ההערה בראש הקובץ
מחייבת לקרוא את כל התחנות אחרי כל שינוי ניווט — זו הפעם השלישית שהוא התיישן.

### 2. Header נקי

"קוד בעלים: X" הוסר מה-header (מידע של לוגין שישב שם קבוע וקיצץ את שם המסעדה) —
עבר להגדרות ⇒ פרטי המסעדה ("קוד בעלים (לכניסה)"). האייקונים קיבלו כיתוב 8.5px:
"יציאה", "הצוות".

### 3. "לצרף את הצוות" — משימה ראשונה כשאין צוות

`teamMembers.length === 0` (אחרי `loadDone`!) ⇒ שורת משימה ראשונה בבית שפותחת את
הגדרות⇒הצוות שלי. נעלמת ברגע שמישהו מצטרף. אומתה על מסעדת הדמו הריקה.

### 4. כרטיס מנה = מטרת הקשה אחת

הכרטיס כולו פותח את העורך (role="button", הכוכב עם stopPropagation); כפתורי
עריכה/מחיקה הוסרו מהכרטיס. **מחיקה עברה לתוך העורך עם אישור דו-שלבי** —
`handleDeleteDish` מעולם לא ביקש אישור, וכפתור מחיקה חשוף על כל כרטיס היה תאונה
ממתינה. אומת מקצה לקצה עם מנת QA (נוצרה⇒נערכה⇒נמחקה, נטו אפס).

### 5. מסלול הלמידה נפתח במשפט אחד

כשהקונפיג תואם את ההמלצה: כרטיס ירוק "הכל מוגדר לפי ההמלצה שלנו" + סיכום שורה +
"שינוי מתקדם" שפותח הכל. ⚠️ קונפיג **מותאם** תמיד מוצג מלא — להסתיר בחירות של
בעלים מאחורי כפתור היה מדווח לו הגדרה שגויה. `showFull = advanced || !isRecommended`.

### סריקת עברית (שני מעברים, ~476 מחרוזות)

- **"הכנסה" ⇒ "כניסה"** במסך הלוגין (הכנסה = income). "בדוק" בזמן טעינה ⇒ "מתחבר…".
- **סיסמא ⇒ סיסמה** בכל האפליקציה (לוגין + הוספת מנהל). "סיסמה של בעלים" ⇒ "סיסמת בעלים".
- **"נסה/י שוב" ⇒ "נסו שוב"** — קול רבים-מנומס אחיד.
- **תיאורי הקושי באונבורדינג היו שקר** — תיארו "קטגוריות נפתחות בהדרגה" שהוסרה
  ב-08-13. עודכנו לסמנטיקה האמיתית של `gate_games` (היקף, לא נעילה).
- **הודעות מוצעות למלצר נוסחו ניטרליות-מגדר** ("ואתה מסודר" ⇒ "והכול מסודר";
  "בוא נסגר" ⇒ נוסח תקין). עבר-נוכח ("הספקת", "נכנסת") זהה לכל המגדרים בכתב.
- "אנשי צוות" ⇒ "חברי צוות" (מונח אחיד). "..." ⇒ "…" במחרוזות עברית (רגקס שלא
  נוגע ב-spread). `console.error(obj)` אחרון תוקן ל-message+code.

## 🎭 2026-08-22 (המשך) — תפקידים במשימות + יישור מינוח עם צד המלצר

תיאום עם סשן המלצרים (שני העדכונים שלהם חיים בפרודקשן):

### בורר "למי המשימה" — `shift_tasks.role`

צד המלצר שואל עכשיו בכניסה "מלצר/ית או ברמן/ית" ומסנן משימות לפי
`role` (NULL=כולם | 'waiter' | 'bar', מיגרציה שלהם `shift_tasks_role_column`).
נוסף אצלנו: צ'יפים **כולם / מלצרים / ברמנים** בטופס משימה חדשה **וגם** בעורך שורה
קיימת; badge תכלת על שורה עם תפקיד. `ROLES` ב-`TasksManager.jsx` הוא החוזה.
⚠️ **`role` חייב לעבור דרך ה-upsert של `persist()`** — אותה מלכודת כמו `expires_on`:
המספור-מחדש כותב את כל השורות, ועמודה שלא נכללת נמחקת בשקט. אומת חי:
bar בהוספה ⇒ waiter בעריכה ⇒ שרד את ה-renumbering.

### מינוח: בוחן ≠ מבחן (החלטת משתמש, הוחל בשני הצדדים)

**"בוחן"** = בוחן קטגוריה / בוחן היכרות. **"מבחן"** = מבחן התפריט המלא בלבד.
תוקן: ספריית המשימות ("לעבור בוחן קטגוריה אחד"), GuidedTour ("בוחני הקטגוריות"),
LearningPathSettings + ProgressChart ("בוחן היכרות"), MemberSheet ("בחנים ומבחנים").

ℹ️ הכתיב "לקבל שולחנות" שנתפס אצל המשתמש היה בשורות ה-DB הישנות (סשן המלצרים תיקן
שם); הספרייה שלנו לא הכילה אותו. FYI מהם: המלצר נשאל כל יום אם הוא במשמרת;
משימות `opening` מוצגות רק לפותחים ו-`closing` רק לסוגרים — `kind` לא מוכר תמיד מוצג.

## 👤 2026-08-23 — פרופיל אישי (שם + מגדר) · חסימת שמות מזויפים · תצוגת מלצר משופרת

### `ProfileGate.jsx` + עמודות `restaurants.owner_name` / `owner_gender`

בקשת המשתמש: "we need name and gender, and from that thats how you treat them."
עברית מטה כמעט כל פנייה למשתמש — בלי מגדר האפליקציה תקועה בלוכסנים וברבים.
- שער חד-מסכי אחרי הכניסה הראשונה: שם פרטי + "איך לפנות אליך?" (בלשון זכר/נקבה).
  "אחר כך" קיים (נשמר ב-localStorage פר-מכשיר) — שער בלי יציאה הוא איפה שסשנים
  ראשונים מתים; דילוג ⇒ ניסוח ניטרלי כמו עד היום.
- מיגרציה `restaurants_owner_profile` — **כולל שני ה-GRANTS ברמת עמודה מראש**
  (select+update), לפי הלקח מ-dismissed_menu_tasks. אומת שרענון אחרי לוגין שורד.
- **שימושים חיים**: `Greeting` — "בוקר טוב, דנה"; שורת ה-header — "מחובר כ…"/
  "מחוברת כ…" דרך `genderWord()` (`src/lib/gender.js`). זו התשתית לכל ניסוח ממוגדר עתידי.
- שער = בעלים ראשי בלבד; מנהל משני (`logged_in_as_name`) כבר מזוהה בשם — מגדר
  עבורו out of scope בינתיים.
- ℹ️ CREWDEMO אופס ל-null אחרי הבדיקות — בודק חנות יפגוש את השער הטבעי (עם דילוג).

### `nameProblem()` — חסימת שמות מזויפים (בקשת המשתמש)

ב-`lib/gender.js`: שמרני בכוונה — חוסם רק מבנה שאין לאף שם אמיתי: ספרות/סימנים,
אות אחת, אות משולשת ברצף, שורות מקלדת (qwer/שדגכ…), ומילות בדיקה (טסט/בדיקה/demo/
asdf…). כל דחייה עם הסבר אנושי. אומת חי: "טסט" ✗, "abc123" ✗, "דנה" ✓.

### תצוגת מלצר: יציאה בלחיצה בחוץ + כניסה אוטומטית (spec)

- **לחיצה מחוץ למסגרת הטלפון סוגרת וחוזרת לניהול** (בקשת המשתמש) — ה-backdrop הוא
  היציאה, המסגרת בולעת קליקים. אומת חי.
- **`?preview=<team_code>`** מועבר עכשיו ל-iframe. 🚧 **צד המלצר צריך לממש**: פתיחת
  סשן צפייה אוטומטי למסעדה הזו — התפריט האמיתי, בלי הקלדת קוד, בלי יצירת חבר צוות,
  בלי כתיבות. עד אז הפרמטר מנוטרל ומוצג הלוגין הרגיל. נשלחה הודעה לסשן המלצרים.

## 🔤 2026-08-23 — השלמות בבריף: קטגוריה על כל הצעה + התאמה חוצת-שפות · "אף פעם לא דף ריק"

שלוש בקשות משתמש באותו סבב, כולן נבדקו חי (dev server, CREWDEMO) לפני push:

1. **"למה זה משלים לדברים שלא קשורים?"** — התברר שההשלמות היו *נכונות* ("חברים,
   הרי גליל" = יין אמיתי בתפריט של סטודיו תל אביב 2026), רק בלתי-מזוהות. כל צ'יפ
   הצעה ב-`TagField` (BriefAssistant.jsx) מציג עכשיו את הקטגוריה שלו
   ("הרי גליל אלון לבן · יינות"), והתאמות מתחילת-מילה מדורגות לפני התאמות אמצע.

2. **התאמה חוצת-שפות** (`src/lib/translit.js`) — המנהל מקליד "לילי" והתפריט אומר
   "Lilly Flower": שני הצדדים מצטמצמים ל**שלד עיצורים** באלפבית משותף (עיצורים
   דומי-צליל מתאחדים: k/c/q, f/p, b/v/w, sh⇒s; c רכה לפני e/i/y ⇒ s; ה בתחילת
   מילה = עיצור h; וו כפולה = v) והתאמה = שלד השאילתה הוא prefix של שלד מילה.
   דטרמיניסטי, בלי AI. מוצג כצ'יפ מקווקו "התכוונתם? Mykonos Tuna · mains" —
   ניחוש נראה כמו ניחוש. ⚠️ **כפילויות מתאחדות רק באיות הלטיני הגולמי**
   (Lilly⇒Lily), לעולם לא בשלד המוגמר — collapse בשלד הפיל את לילי ("ll"⇒"l",
   קצר מדי) ויצר התאמת-שווא גליל⇒Galaktoboureko. נבדק ב-13 מקרים ב-node ואז חי.

3. **"שלא יהיה לו דף ריק תמיד"** — כשכל המשימות בוצעו, צצה משימה חדשה:
   "לבדוק את התקדמות הצוות" (פותחת את מסך 📊). ה"בוצע" שלה = המנהל פתח אותה
   היום (localStorage יומי `menu-app-owner-progress-<rid>-<date>`, אותה תבנית
   כמו ה-redo). מופיעה רק כש-`ownerTasks.every(done)` — כלומר גם משימות תפריט
   פתוחות דוחות אותה (הדף ממילא לא ריק); אחרי שנפתחה היא נשארת (כבוצע) עד סוף
   היום גם אם משימה אחרת נפתחה מחדש.

בנוסף: בקשת המפעיל הממתינה מ-20.8 ("שולח קובץ xxxxxx", סטודיו תל אביב) נסגרה
כ-done — המשתמש שאל מה זו ההודעה; זה טקסט שהוקלד בבדיקות, אין קובץ מצורף.
ℹ️ ל-CREWDEMO נוספו כתוכן דמו קבוע: בריף להיום + 17 משימות צ'קליסט
(פתיחה/סגירה/כללי שירות מהספרייה המומלצת) — תוכן ריאליסטי לבודקי החנויות,
לא נתוני QA למחיקה. `dismissed_menu_tasks` אופס חזרה ל-`{}` אחרי הבדיקה.

## 📨 2026-08-23 (המשך) — הודעות לצוות: נוסח פורמלי · שידור לכולם · תיבת "תרצו להוסיף?"

1. **נוסחים פורמליים** (`TeamMessageDialog.jsx`): המשתמש — "בסוף זו הודעה מהבוס".
   כל חמשת הנוסחים המוכנים שוכתבו לרישום פורמלי, בלי אימוג'ים ("נא להשלים את
   הלימוד היומי..."). עדיין ניטרליים מגדרית (גוף שני עבר).
2. **הודעה לכל הצוות**: כפתור 📣 "הודעה לכל הצוות" בראש מסך הצוות ⇒ אותו דיאלוג
   במצב broadcast — צ'יפים של כל הנמענים (כולם מסומנים, הקשה מסירה), אותה הודעה
   נכתבת **כשורה נפרדת פר-נמען** ב-`team_messages` (כדי שאישורי הקריאה
   הפר-מלצריים ימשיכו לעבוד). נוסח חדש ראשון: "נא לקרוא את העדכון היומי...".
   נבדק חי: שידור ל-17 חברי CREWDEMO ⇒ 17 שורות ב-DB (נמחקו אחרי הבדיקה).
3. **"תרצו להוסיף משהו באפליקציה?"** — סקשן חדש בסוף ההגדרות. `OperatorLine`
   קיבל props‏ (`title`/`placeholder`/`prefix`/`showPending`) ומשמש גם שם;
   הבקשה נכנסת לאותו תור מפעיל עם קידומת `[הצעה לאפליקציה]`. נבדק חי כולל
   שורה ב-`operator_requests` (נמחקה).

**שאלת המשתמש "למה לא קיבלתי בטלפון?"** — ההודעות נשמרות ומוצגות בתוך אפליקציית
הצוות (ManagerMessages בצד המלצר, חי בפרודקשן) — **אין שום push notifications**;
כפתור ההעתקה לוואטסאפ הוא הערוץ המיידי. אם ירצו push אמיתי — פרויקט נפרד
(APNs/FCM בעטיפות הנייטיב).

## ✅ 2026-08-23 — תיקון קריסת פרודקשן + "רק מאשרים": הבריף מתמלא מאתמול

🔴 **קריסת פרודקשן שנתפסה ע"י המשתמש**: "TagField is not defined" — מסך שגיאה מלא
בכל פתיחת עריכת בריף קיים. השורש: `DailyBriefEditor` (בתוך OwnerDashboard) משתמש
ב-`<TagField>` אבל ה-import הביא רק את ברירת המחדל של BriefAssistant. **נכנס עוד
ב-`93e02f0` (22.8, סבב ההקלדה החופשית)** — ה-QA של אותו סבב בדק רק את האסיסטנט,
לא את מסלול העריכה-של-בריף-קיים. תוקן ב-`443ae1f`. אותה מלכודת בפעם הרביעית:
**build ירוק אינו מוכיח כלום על זהויות JSX — לבדוק כל מסלול רנדור חדש בדפדפן.**

⭐ **עיקרון חדש בפנקס השותף (מס' 8): אוטומציה לפני עבודה** — המערכת ממלאת, המנהל
מאשר. מימוש ראשון: **BriefCarryOver** (ב-BriefAssistant.jsx) — ביום חדש עם בריף
ריק, העדכון האחרון (בד"כ של אתמול) מוצג מוכן: צ'יפים של חסר/ממליצים/בהכנה/הערות,
כפתור "אישור ושליחה להיום" (כותב את אותו תוכן להיום), "עריכה" (זורע את הטיוטה
ופותח את העורך הידני), X חוזר לאסיסטנט. שורת המשימה אומרת "העדכון של אתמול כבר
מוכן — נשאר רק לאשר". **שום דבר לא נשלח בלי לחיצת המנהל.** נבדק חי מקצה לקצה
(CREWDEMO: הבריף הוזז לאתמול ⇒ הכרטיס הופיע ⇒ אישור ⇒ שורת היום ב-DB ⇒ המשימה
ירוקה).

## 🚪 2026-08-23 — יציאה מצ'קליסט אינה השלמה

"נכנסתי רק להסתכל וזה נהיה ירוק — והמנהל שוכח" (המשתמש). `onExit(completed)`
ב-TasksManager: חץ החזרה מעביר `false` (הצצה — המשימה נשארת פתוחה), רק
"שמירה וסגירה ✓" מעביר `true` ומנקה את דגל ה-↺. ה"בוצע" הנגזר (יש שורות
בצ'קליסט) לא השתנה — הגדרה ראשונה עדיין משלימה מעצם קיום השורות.
נבדק חי: ↺ ⇒ פתוח ⇒ כניסה+חץ חזרה ⇒ עדיין פתוח (3/4) ⇒ כניסה+כפתור ירוק ⇒ 4/4.

## 👀 2026-08-23 — "מי למד היום": שתי רשימות בלבד, בלי האפורים

בקשת המשתמש: "מי שלא למד — ברור מאליו; תוריד את האפורים... אחת עם מי שלמד ואחת
עם מי שקרא את העדכון או סימן משימות; אם אין — תגיד שאף אחד". `LearningStatus.jsx`:
- נשארו **שתי** קבוצות: "למדו היום" (ירוק, ממוין לפי דקות) ו"קראו את העדכון או
  סימנו משימות" (תכלת; חדש — נשען על `daily_brief_reads` להיום + `shift_task_done`
  להיום; מי שלמד מנצח ולא מופיע פעמיים). קבוצות "נכנסו אבל לא למדו" ו"צריכים
  ללמוד" הוסרו. קבוצה ריקה מציגה שורה מפורשת ("אף אחד עדיין לא...").
- 🔴 **אי-התאמה שנתפסה תוך כדי ותוקנה**: כותרת מסך הצוות אמרה "0 מתוך 17 למדו"
  בזמן שהכרטיס אמר 12/17 — הכותרת נגזרה מ-`leaderboard.last_study_date` והכרטיס
  מ-`progress_snapshots`. עכשיו `studiedToday` בדשבורד מעדיף את שורות
  `LearningStatus` (דרך `onRows`/`liveByMember`) כשהן קיימות; ה-leaderboard נשאר
  fallback לפני הטעינה. שוב הדפוס: שני מסכים שחולקים על אותו מספר = הבאג.

### תיקון המשך (2026-08-23): קבוצת "קראו/סימנו" אינה מסוננת מול הלומדים

המשתמש תפס סתירה חיה: מלצר שקרא את הבריף ב-11:35 וגם למד הופיע רק ב"למדו",
ושורת "אף אחד עדיין לא קרא" סתרה את לוח הקריאה מתחתיה. הקבוצות כבר לא זרות —
מי שלמד וגם קרא מופיע בשתיהן. (e37aa9a)

### הסבר אירוח מובנה לסטודיו תל אביב (2026-08-23)

"איך אנחנו מארחים כאן" של 87EE49 היה בלוק טקסט אחד עם מפרידי `|`. נכתב מחדש
ב-DB עם כותרות סעיפים (ווק/סלטים/תוספות/סושי/בר) ושורות תבליט, ובצד המלצר
נוסף `whitespace-pre-line` ל-WelcomeTutorial (commit 3b440db בריפו המלצרים —
נדחף מכאן אחרי pull, נשלחה הודעה לסשן המלצר). ⚠️ בשכתוב הראשון הוכנסה בטעות
שורה שלא במקור ("אין אקסטרה חריף") — נתפסה ותוקנה מיד; שכתוב תוכן = העתקה
נאמנה, בלי להמציא.

## 🎓 2026-08-23 — קוד מלצרים מתחילים (לימוד בלבד), נבנה בלופ /loopy

**בקשת המשתמש**: קוד כניסה נפרד למלצרים מתחילים — אותו אתר צוות, בלי משימות
יומיות; רק לימוד תפריט. מומש בשני הריפואים + DB, נבדק חי בלופ (3 סבבים).

- **DB** (מיגרציות `restaurants_trainee_code`, `team_join_trainee_support`):
  `restaurants.trainee_code` (unique, hex 6, backfill לכולן + טריגר
  `ensure_trainee_code` לחדשות + `gen_trainee_code()`); ‏`team_join` מקבל גם את
  קוד המתחילים ומחזיר `trainee: true/false`. grant select על העמודה ל-anon
  **לפני** הוספתה ל-RESTAURANT_COLUMNS (המלכודת המתועדת).
- **owner**: כרטיס "קוד מלצרים מתחילים" (תכלת) ב"הצוות שלי" עם העתקה והסבר.
  🔴 **תוקן שורש בדרך**: לוגין טרי מסר את פרופיל ה-RPC החסר (בלי owner_name/
  tracked_flags/trainee_code…) — עכשיו OwnerLogin שולף אחרי הטוקן את השורה
  המלאה ב-RESTAURANT_COLUMNS וממזג (`{...rpc, ...full}` — logged_in_as_name
  קיים רק ב-RPC). עד עכשיו כל "יום ראשון אחרי לוגין" רץ על אובייקט חלקי.
- **waiter** (commit `98c388e`): `session.trainee` מהצטרפות; במצב מתחיל —
  ניווט 2 טאבים (תפריט·תרגול), דילוג על שערי תפקיד/משמרת/בריף, נחיתה בתרגול,
  בלי סיור-המשימות (הצעדים שלו מצביעים על טאב שלא קיים), ו-effect שמחזיר
  לתרגול כל ניסיון נחיתה על טאב נסתר (הסיור סיים ב-setTab("home") — נתפס חי).
- קודים לדוגמה: CREWDEMO ⇒ FEEA61 · סטודיו ⇒ DBCFBB. חברי QA נמחקו.

## 🏛️ 2026-08-24 — "אודות המסעדה" בצד המלצר (commit c44b256 בריפו המלצרים)

המשתמש: מדריך האירוח לא צריך להיות מסך הפתיחה. בוצע בצד המלצר (מהסשן הזה,
בתיאום — רק קבצים שלי): מסך הפתיחה מציג טיזר קצר בלבד; המדריך המלא עבר למדור
"אודות המסעדה" — כרטיס בראש טאב התפריט שפותח מסך מלא (שם, סוגי מטבח, תיאור,
מדריך אירוח pre-line). בנוסף: מדריך פתיחה מותאם למתחילים (בלי שורת המשימות,
מסיים ב"מתחילים מהתפריט"). ל-CREWDEMO נוסף מדריך אירוח דמו (סעיפי פתיחה/
ארוחה/בר/סיום) — תוכן קבוע לבודקים. QA מלא בשני המצבים; חברי QA נמחקו.
🚧 רעיון המשך שהמשתמש זרק ("maybe questions about it"): שאלות על כללי האירוח —
טרם עוצב; ההצעה: המפעיל/AI מחלץ שאלות מהמדריך פעם אחת והמנהל מאשר (עיקרון 8).
